# 验收未完成量开发知识变化候选

## 来源身份

新实现事实来自本地实验工程 `quality-platform-lab@26db0e1c98657cd120430cec0b92964c888cacd5`，父提交为 `35948aebf41b8772b77df18bd68ce9ff2d295344`。正式质检知识输入固定为 `omni-brain-harness-quality-check-v1@900cf85`；它不是该实验工程的当前实现副本，本阶段保持只读。

## 稳定变化

1. 任务分析表的默认请求开始消费既有 `acceptance.pending`，并在任务、日期、组、标注员四级显示同一服务端聚合指标；
2. Vue 的统一行转换新增 `acceptancePending`，表格列复用既有排序、数字区间筛选、列配置和下钻，没有前端公式；
3. 默认指标从 10 个变为 11 个；为继续支持 5 个固定问题选项，共享最大指标数从 15 最小调整到 16；
4. 固定 3024 行现场的四级结果为 1737、144、51、20，且 16/17 项接口边界和 Edge 用户旅程已有新鲜证据。

## 建议规范落点

- 若父知识候选已经登记 `quality-platform-lab`，则**原位更新**其系统实现页和来源页到提交 `26db0e1`；若尚未登记，则创建独立系统页，不能并入名称相似的历史 Ratio 原型；
- 原位更新人工质检任务分析工作台的软件纵切：说明既有指标如何经过请求、统一转换和表格消费，并记录 11+5 的当前契约；
- 更新验收工作旅程视图，在“跟踪验收进度”节点增加直接查看和下钻未完成量的入口；视图只链接规范页，不复制指标实现；
- 业务口径页只需要补充当前实验实现证据或实现链接。`acceptance.pending` 的定义本次没有改变，不应伪装成新增业务规则。

## 保持边界

- 不把本地实验工程描述为生产系统；
- 不改变验收完成率、配额、分配缺口或通过率定义；
- 不推导风险阈值、告警等级、责任人、催办或自动执行；
- 不声称 1737、144、51、20 是其他日期或环境的永久业务事实；
- 正式知识发布仍需比较父版本、内容审查和人工批准。

## 证据

- 实现：`quality-platform-lab@26db0e1c98657cd120430cec0b92964c888cacd5`；
- 验证报告：同提交 `docs/acceptance-pending-visibility-verification.md`；
- 独立真值：固定用例的 `expected.sql`；
- 运行：PostgreSQL、FastAPI、Edge、全量 Python/Vue、类型与构建；
- 参考状态：`reference_candidate`。
