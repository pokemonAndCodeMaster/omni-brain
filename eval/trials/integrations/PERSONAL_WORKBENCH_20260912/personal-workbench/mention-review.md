# 原生事项引用定点复核

2026-09-13。结论：**修复后的原生引用规则和事项分组可以交付，本次复核范围内无剩余阻断。**

本次只检查 `scripts/render_personal_workbench.py` 的 `<issue>` 转换和 Todo / Backlog 分组，没有改代码、重跑无关测试、重新生成或上传网页。

## 首次发现与修复结果

1. 原解析器忽略注释、处理指令等事件，`<issue ...>YYH-12<!--keep me--></issue>` 和 `<?keep me?>` 被当作纯引用接受，附加内容静默消失。修复后记录并拒绝这些事件；注释、处理指令、声明和 CDATA 四个例子均原样保留为安全转义文本。
2. 原规则接受 `.../issue/YYH-12/../../issue/YYH-13`，显示的编号与浏览器实际目标不一致。修复后拒绝解码后的点路径段与反斜杠；普通遍历、`%2e%2e`、反斜杠及 `%5c` 四个例子均不再转换为链接。

定点执行的八个拒绝案例全部通过。正常引用仍生成 HTTPS Linear 链接，前后正文保留；行内代码、围栏代码和转义示例不会被转换。HTML 渲染仍关闭，额外事件属性、重复属性和非 Linear 主机不会通过原生引用白名单。

分组检查：`Todo / unstarted → now`，`Backlog / backlog → ideas`，与当前流程含义一致。

## 版本与证据

- 最终复验的渲染器 SHA-256：`6e7d1226b298fb8c4a3f18dd7a08769279d3befcd0ea8c30f61881c28c1f284b`。
- 首次读取的最终页面 SHA-256：`60785107a98b886a0354ba1ebb56ac3c1c8b0fea2f2690c653fa885c4da352c5`，与 `final-browser-check.json` 一致。
- 该页面 50 个真实原生引用均已转换，无 `<issue>` XML 残留；原始全文数据仍保留在页面数据区。
- 本次修复针对异常输入；最终上传页面与修复后渲染器对全部 56 条正文的等价性，由主代理另行核对，不在本报告中冒充已验证。
