# 共作：以持续上下文为核心的个人与团队工作台

> 产品定义与交互候选 v0.3 · 2026-09-05
>
> “共作”为原型占位名。本轮提出可讨论的实体与交互，不是已批准的生产实现规格。
>
> 依据分为三类：用户本轮明确的使用环境与约束；已有《团队协作工作台：产品定义与体验讨论稿》和知识/Harness 材料；截至本日查询的 Linear、OpenCode、OpenAI 官方资料。下述产品设计是本轮建议，不冒充既有材料中的已决结论，也不声称外部产品已经实现全部设计。

## 一、产品定义与目标

**产品定义：以工作事项及其持续上下文为中心，让人能够组织工作、委托 AI、检查成果，并把经验转化为后续可复用能力的个人/团队工作台。**

产品管理的是“这件事”，不是“某个 Agent 的这次会话”。用户可以只记录一个念头、提出一个问题、完成一次学习或推进正式交付，不要求所有输入经过统一研发流程。

长期愿景是大多数团队常见工作都能够委托给合适的 AI 能力完成主要执行，并交出可接受的成果。90% 不宜理解为“90% 的任务调用过模型”或“90% 的代码由模型生成”；应观察可委托完成率、人工补背景与接管负担、结果质量，以及能力复用效果。

### 已有条件，不再作为本轮选型题

团队采用 OpenCode，存在内源版本，并可以配置公司模型 URL/API Key。用户已经准备了含人工、自动化和大模型质检依赖的 Ubuntu20.04 镜像；成员 Ubuntu 工作站能够运行容器；中心机统一管理执行机与 Run。代码多数已进入 CodeHub 的质检团队 Git 仓，Markdown/llmwiki 已存在但还需审视。

个人采用 Windows + WSL Ubuntu22.04，主要通过 VSCode Codex 插件工作，也用 OpenCode 做对照实验；有 GitHub 中心管理仓和独立项目仓。个人希望管理学习、工作、爱好与游戏，而不仅是软件开发。

本产品复用这些条件，不重新实现模型工具调用循环、多 Agent 执行内核或现成 Skill 加载机制。

## 二、产品实体：少量主对象，明确关系

### 2.1 直接面向使用者的对象

| 对象 | 定义 | 必要性与页面位置 |
|---|---|---|
| 灵感 / 线索 | 尚未形成明确工作承诺的原话、问题或反馈 | 快速记录和讨论；可以不启动任何执行 |
| 工作事项 | 有可理解目标、责任人和持续上下文的一件工作 | 核心协作页面；需求、研究、修复、学习、复盘是工作类型 |
| 专题 / 阶段项目 | 为一个阶段结果组织若干事项，有完成或结束条件 | 工作事项中的专题标签页，不要求所有事项归入专题 |
| 领域 | 长期持续关注或负责的范围 | 如人工质检、学习、游戏；默认作为关联与筛选 |
| 资源 / 资产 | 工作引用、修改或验证的实际对象 | 知识页、代码仓、模块、系统、数据集、报告等，通常在事项侧栏与知识入口 |

工作事项的核心字段可以理解为：身份、标题、类型、责任人、目标、范围、当前状态、时间，以及关联对象。轻量事项不要求填写所有可能字段。

需求不是另一份需要平行维护的任务正文。一般情况下，一个正式需求直接成为“需求”类型的事项；本轮希望交付的结果写在其中。只有多次交付确实需要独立安排和接受时，才分阶段或拆子工作。

灵感和正式需求之间允许多对多来源关系：多个线索形成一个需求，一个线索也能产生多个工作。不因转为需求而删除原始讨论。

### 2.2 支撑协作、但不应全部做成导航入口的对象

| 对象 | 管理什么 | 为什么不能只写成备注 |
|---|---|---|
| 上下文记录与修订 | 当前共识、依据、未决、提案与历史 | 会被多个参与者复用，需要版本与来源 |
| 子工作 / 贡献 | 谁具体完成哪一部分 | 可以独立分派，但引用上层背景而不是复制一份需求 |
| 成果及验证证据 | 实际形成的内容和能证明什么 | 有自己的版本、运行依据和接受状态 |
| 委托与 Run | 一次目标、执行器、原生会话、环境和结果 | 一件工作可能经历多次尝试，不能用 Run 状态代替业务状态 |
| 复盘与能力改进候选 | 来自何处的问题、拟改对象和验证 | 必须区分提议、验证与真正发布 |
| 呈现配置与会议实例 | 阅读规则、本次议程、读取快照和会中决定 | 既不复制当前工作事实，又能保留当时汇报内容 |

“对象存在”不意味着“首页多一个菜单”，更不意味着第一版必须建立复杂的通用图数据库。

### 2.3 关系挂接的准确含义

例如 QC-101“解释分配不足”，可以关联两个专题、一个长期领域、分配与快照两个模块，记录一个结果协调人和若干具体贡献，并挂接方案、运行、页面候选与验证。

这些不是八份任务，而是围绕同一事项建立不同语义的联系：服务哪个目标、谁负责什么、影响什么、依据是什么、由哪些结果证明。

不能仅有一串没有语义的 related IDs。至少要分清“服务于”“组成”“依赖”“影响”“产生”“验证”“来源于”。也不必立即把这些语义抽象成一个万能关系引擎。

对外部系统还要区分三层能力：

1. 关联定位：知道资源身份与地址，能够打开；
2. 状态读取：接口能读取外部事实，并显示更新时间与读取失败；
3. 动作执行：有接口与权限执行编辑、触发、合并等，并回查实际结果。

保存一个 CodeHub 链接不会自动获得 MR 编辑与同步能力。Git 访问可用也不能证明内部 Webhook/API 已经可用。

## 三、持续上下文：本产品的核心内容空间

### 3.1 共享的不是同一条聊天，而是共同工作的状态

上下文分为四种内容：

| 内容 | 示例 | 产品动作 |
|---|---|---|
| 当前确认内容 | 为什么做、目标、范围、关键决定 | 阅读、引用、提出修订 |
| 依据与材料 | 知识、源码身份、工况、原始反馈 | 查看用途、来源和版本；必要时核验 |
| 候选与未决 | 另一方案、待确认口径、尚缺证据 | 讨论、比较、采纳、保留分歧 |
| 过程与历史 | 旧决定、旧方案、失败尝试和成果 | 按时间或变更回看、引用给后续工作 |

长期知识与事项上下文分别维护。上下文可以引用知识版本并解释用途，不应把知识全文复制到每个需求里。原始讨论可以完整归档，而摘要不是替代所有原始材料的唯一证据。

### 3.2 需求、子工作、会话与运行怎样相连

一个需求可维持一份共享背景，设计、开发、验证、人工审阅分别使用自己的会话。会话和 Run 引用需求身份与当时上下文版本；需要时增加自己的局部事实，但不要私自覆盖共同目标。

大工作可以有局部上下文：项目/领域知识 → 需求共同背景 → 子工作关注点 → 本次运行输入。这里是继承与引用，不是每层复制一份大文档。

对外显示的持续上下文与模型单次可容纳的输入不是一回事。模型仍然需要读取或获得相关内容；不同会话并不会天然共享完整缓存。目标是减少重复组织、反复提问和失联，不承诺任意新 Agent 都能零成本获得全部理解。

### 3.3 一条有产品意义的交互

当前上下文 v3 已确认只解释结果、不改变分配规则。页面讨论提出“数据缺失”和“数据过期”不应混为一谈。新的表述作为候选高亮，显示原文、建议和依据。责任人采纳后形成 v4；正在运行的验证仍显示它基于 v3，并提示有新共识。

用户查看差异后，明确继续采用旧范围、同步新共识，或者暂停受影响工作。不是把 v3 在后台悄悄改成 v4，也不是所有运行从头重来。

产品需要的功能因此很具体：当前内容阅读、局部讨论、提案差异、版本、依据引用、依赖提示、进入运行时的上下文摘要，以及归档后再检索。

原型已体现候选采纳、版本号、历史记录与运行的旧依据提示；尚未实现完整历史正文恢复、并发编辑合并或自动影响分析。

## 四、项目、业务方向与模块：不要再当成一棵树

### 4.1 三种问题，三种概念

**领域回答：我们长期在哪一块积累与负责？** 人工质检、大模型质检、机器学习、游戏都可以是领域。它不因本月任务结束而消失。

**专题回答：这一轮共同想拿到什么结果？** “9 月跑通评测首个闭环”“完成验收解释能力”“两周内学懂一种方法”可以成为专题。日期早期不确定时允许阶段时间或结束条件，不必捏造精确截止日。

**模块/资产回答：实际建设、修改和运行的对象是什么？** 验收分配模块、快照服务、评测模块、知识前端、一个代码仓等属于这一类。模块是实现边界，不等于业务方向。

一个模块可能支持多个领域；一个领域也会跨越多个模块。有时两者名称相同，仍应根据含义区分“长期业务责任”和“具体系统对象”。

### 4.2 对用户列举情况的判断

| 名称或情况 | 建议表达 | 原因 |
|---|---|---|
| 人工质检、大模型质检 | 长期领域 | 不宜用永不关闭的项目冒充持续职责 |
| 有目标、有节点的近期攻关 | 专题 / 阶段项目 | 需要共同结果、安排与收口 |
| 和别的团队合作完成一次能力 | 根据复杂度是事项或专题；另记协作方 | “跨团队”是关系，不是独立项目类型 |
| 持续上下游合作 | 领域/系统关系与持续协作记录 | 没有自然终点，不必强建一个项目 |
| 验收分配、快照模块 | 关联资产/模块 | 用来定位实现与影响范围 |
| 本月几块业务的阶段目标 | 对相应领域或专题登记本期目标与证据 | 不必第一版就增加完整 OKR/Initiative 层级 |

前端默认只保留一到两种组织方式。模块通常从事项侧栏进入；只有团队真实反复需要模块总览时，才固定成一个视图。

## 五、组会是“议程化的阅读”，不是遍历所有维度

### 5.1 固定菜单与可变呈现的区别

个人工作台用于发现自己应做的贡献与待决定事项；工作事项用于统一跟踪；会议是对这些内容的一次有顺序、有阅读粒度的呈现。

不是把个人、项目、领域、模块、时间全部做成一级导航，然后要求组会依次打开。

一个呈现配置需要说明：范围、时间窗、对象粒度、分组、显示字段、哪些内容完整展开、哪些内容仅做摘要，以及跨板块去重方式。这是保存“怎么看”，不是保存另一份当前事实。

### 5.2 建议的默认议程

| 板块 | 主要内容 | 不重复呈现什么 |
|---|---|---|
| 需要决定的事 | 阻塞、取舍、需要协调、负责人需要的输入 | 已有事实就地引用，不重新口述全部历史 |
| 专题目标与缺口 | 阶段目标、变化、整体缺口、关键依赖 | 不重新逐个念刚讲过的事项 |
| 近期交付与变化 | 前面尚未完整讨论的近期成果 | 不重复展开相同事项 |

需要按业务方向或个人过工作时，替换某个板块的阅读规则，不是追加三轮同样的任务列表。

“相同事项只完整讲一次”不是“这个事项在其他语境下不得再出现”。例如已经讲过一个共享能力的开发进度，另一个专题仍有独立的接入决定，那么保留该决定与关联，折叠重复的进度。去重的对象是重复讲述，不是不同的业务问题。

### 5.3 会前、会中、会后各产出什么

会前由配置形成实时预览；需要留存时冻结本次读取的事实快照。会中从重点条目下钻证据、记下决定或后续工作，直接关联原事项。会后得到带来源的纪要、决定与行动，不要求把会议记录再手动搬回任务。

正式产品可以让同一呈现规则输出浏览器演示、PPTX 或 PDF，但导出的文件是带生成时间的快照，不是另一份需要持续编辑的任务事实。

原型已经包含浏览器演示、议程顺序与开关、跨板块首次展开去重、事实快照和 Markdown 纪要；尚无 PPTX 导出或任意报表配置。

## 六、复盘与进化：从文档结果转成可发布的能力变化

### 6.1 可以称为进化，但不是模型权重自动训练

此处“进化”是有证据的工作能力改进：从真实任务中观察问题，提出可复用修改，验证效果并受控发布，让下次工作以新方式进行。

单纯留下一篇复盘属于经验保存；如果复盘改变了被后续任务实际采用的知识、Skill、配置或流程，才形成完整闭环。

### 6.2 不同收获有不同落点

| 复盘发现 | 优先落点 |
|---|---|
| 某项业务规则或系统事实此前未知 | 知识页 |
| 一种稳定、可复用的操作方法 | Skill |
| 角色分工、工具权限、模型配置不合适 | Agent 配置 |
| 路由过重、上下文装配不对、反馈时点不合理 | Harness / 工作方式 |
| 环境准备或工具包装本身有问题 | 工具/运行环境 |
| 模型本身在某类任务反复失败 | 模型能力评估与选型；不是随便改提示词就宣称解决 |

不默认每条经验都创建新 Agent，也不自动把一次成功泛化成任意业务均可使用。

### 6.3 产品对象是“能力改进候选”

候选记录来源事项与运行、具体问题、修改对象和版本、修改内容、希望改善的行为、验证案例，以及评估结果与是否发布。

事项中显示“这次学到了什么、建议改哪里”；维护中心显示跨事项的候选、重复问题、版本比较、回归与发布。普通使用者不需要了解候选最终对应哪个框架目录。

对轻任务不强制完整复盘。反复失败、人工纠偏、特别有效的方法或重要交付可以触发候选整理。发布到个人与团队两个环境是两个动作；不得把公司数据随通用能力一起自动导出。

## 七、Linear：具体借鉴哪些理念与交互

本轮依据官方文档进行产品分析，不是完整源码审计或登录后的全量试用，也不假定可以直接获得其完整产品用于内部部署。

### 7.1 对象有明确语义，而不是所有概念都叫任务

Linear 的基础工作单元是 Issue；Project 组织共同交付，Initiative 组织更高层目标，Cycle 是重复计划周期，View 是对同一事实的不同阅读。[L1]

值得借鉴的是语义清楚，不是把整套层级全搬过来。本产品第一版不必建立 Initiative/Project/Epic/Requirement/Task 的五层必经结构。

### 7.2 项目与视图不是一回事

Linear 鼓励 Project 有明确结果或时间边界。Initiative 用于有意组织的一组项目；按过滤条件自动汇聚内容应使用 View。[L2][L3]

对应本产品：阶段专题拥有共同目标，领域视图只是按长期责任查看工作，不能混用。

限制也必须看清：Linear 当前文档写明单个 Issue 只能关联一个 Project；多团队 Project 并不等于任意多项目归属。本产品保留“同一事项服务多个专题”的明确关系，不照抄该限制。[L2]

### 7.3 文档就地关联工作，变化可以检查

Linear 支持把文档关联到工作对象，协同编辑、行内评论、版本历史及单独标记 AI 改动。[L4]

对应本产品：方案是事项中的活内容，评论锚定段落；采纳建议时看差异与依据，而不是收到一份完全重写、失去讨论历史的新文件。

### 7.4 人仍负责，AI 被委托

Linear 把个人责任人与 Agent 委托分开。其 Agent 使用工作区现有事项、文档与历史，并可在具体评论内被调用。[L5][L6]

对应本产品：委托入口在事项里。常用能力作为动作出现，Agent 配置留给维护者。读取共同工作事实不等于把所有会话混为一条。

### 7.5 进展与组会从工作事实生长

Linear 项目更新组合健康判断与正文；官方说明包括根据上次更新后的变化生成草稿，再由人补充与发布。其团队也描述了直接在 Linear 中阅读项目更新进行周度同步的做法。[L7]

对应本产品：自动生成变化摘要，人补充原因、风险和决定；会议读取同一事实，不再要求复制一份手工周报。

### 7.6 真实使用后再形成共享能力

Linear 文档明确描述从有效对话保存个人 Skill，再形成团队共享 Skill，也提供按事件或时间触发的 Loops。[L6]

这支持“从使用中抽取方法”的产品路径，但不能据此声称 Linear 已实现本设计的所有跨版本回归、能力进化或内部环境调度。

### 7.7 编码执行可以复用现成工具

Linear 的 Coding sessions 使用 Claude Code 或 Codex，并把 Issue 上下文、开发环境、PR 和验证产物连接起来；现有文档要求 GitHub 集成，环境也有单仓限制。[L8]

对你最有价值的结论不是换成 Linear 的工具链，而是保留工作台与执行器的责任边界，用 OpenCode 与你已有的容器承载同类体验。

### 7.8 少跳转与保留阅读位置

Linear 的 Peek 允许在列表或看板中预览事项与项目，并用键盘继续浏览相邻对象。[L9]

原型采用速览弹窗表达相同收益：组会中看一次依据后回到原议程，不为阅读一个事项丢掉当前视角。正式实现还应补齐列表滚动和阅读位置恢复。

## 八、同一产品核心，个人与团队分别配置和部署

### 8.1 共同核心

工作事项、持续上下文、资料引用、成果、运行、验证、复盘、能力版本和呈现方式相同。需求、学习、研究、实验和游戏复盘具有不同的内容模板，不是不同产品。

个人版不是只换名字的团队版：默认责任人为自己，隐藏不需要的多人协作动作，研发字段按需出现。团队版增加分享、角色、责任分工、审阅与团队议程。数据与凭证独立，通用功能共享一套代码。

### 8.2 部署映射

| 层 | 团队内部 | 个人使用 |
|---|---|---|
| 控制端 | 中心机管理工作事实、派发与监控 | 可直接运行在自己的 WSL |
| 执行端 | 成员 Ubuntu 工作站 | 自己的 WSL，本地一个执行节点 |
| Agent 执行器 | 现有 OpenCode 封装 | Codex 和/或 OpenCode 接入 |
| 环境 | 已准备的 Ubuntu20.04 业务镜像 | 本地环境或按任务使用容器 |
| 仓库 | CodeHub 团队单仓 | GitHub 中心仓和若干独立仓 |
| 知识 | 团队 llmwiki | 个人知识入口与跨仓引用 |

公司部署不依赖外网个人订阅。个人订阅也不应变成整个团队共用的认证来源。

Codex 官方资料说明本地工具支持 ChatGPT 订阅登录和 API Key 两种路径；API Key 使用按 API 计费，不等于消费 ChatGPT 会员额度。[O1] App Server 是供自有客户端集成认证、会话历史、审批和事件的官方接口，个人版可以评估复用它，不必从 VSCode 界面抓取执行记录。[O2] 具体后台认证方式仍需结合实际安装版本与使用条件验证。

### 8.3 中心仓不必成为所有代码的物理容器

个人中心管理仓可以承载通用 Harness、知识入口、项目登记与个人工作组织；项目代码继续留在自己的仓库。关键是资源有清楚的仓库身份、相对路径和版本，而不是强制复制全部代码。

知识、任务事实与管理界面也不应形成三套都需人工修改的正文。已有 Markdown 内容继续由 Git 管理，工作台负责阅读、关联和受审变更入口。最终业务状态使用何种存储，属于后续工程设计，不把知识不用数据库误推为整个产品禁止数据库。

## 九、保留既有 OpenCode 方案，只补产品所需的连接

OpenCode 官方 Server 文档提供 HTTP/OpenAPI、会话、状态、消息、异步输入、中止、权限响应和事件能力。[R1] 这说明用外层工作台调用现有执行器有官方接口基础；**内源分支实际支持哪些接口，仍须以安装版本为准**，不能拿上游文档直接当内部契约。

产品只需要明确四类身份：工作事项 ID、持续上下文版本、委托/Run ID、执行器原生会话 ID。执行记录再关联机器、环境、目录、分支或成果版本。

上下文、Run 和环境不是同一生命周期。一项需求的上下文可以长期保留；多次串行工作可以复用仍然有效的环境；互相独立或有冲突的尝试使用独立检出与容器。不要为了每次提问都启动容器，也不要让并行修改无控制地写入同一个工作目录。

普通成员只需知道本事项用哪个运行、现在等什么、有哪些结果以及怎样暂停/接手。执行机注册、健康、容量、接单与回收集中到维护中心。容器、目录及独立 HOME/会话数据的隔离策略，需要在真实执行端实现，不因界面显示“独立环境”就已经成立。

## 十、原型页面与具体操作

| 页面 | 主要对象 | 可以体验的动作 |
|---|---|---|
| 我的工作 | 待处理动作与关注成果 | 记录灵感、打开上下文变化、进入事项 |
| 工作事项 | 统一工作记录 | 查看、按专题或领域分组、关系编辑、进入专题 |
| 灵感与讨论 | 原始线索 | 保存、讨论、形成后续事项 |
| 事项 / 概览 | 目标、范围、贡献 | 看清结果与分工 |
| 事项 / 共享上下文 | 当前共识与候选 | 查看依据、提出修订、采纳、查看版本 |
| 事项 / 成果与验证 | 产物与证据 | 打开示例成果、补充模拟验证、接受结果 |
| 事项 / 推进记录 | 变化与运行 | 查看委托、原生会话、环境和旧依据 |
| 事项 / 复盘与成长 | 经验与候选 | 形成改进、进入维护与回归 |
| 会议 / 周回顾 | 呈现配置与会议实例 | 调整议程、冻结、演示、回写记录、导出纪要 |
| 知识 | 当前可浏览内容 | 阅读材料与来源边界，提出修订入口 |
| 维护中心 | 能力、执行机、Run、连接 | 管理候选，模拟接单控制与运行查看 |

一级导航只保留“我的工作、工作事项、灵感与讨论、知识”，固定呈现单独列出组会或个人回顾，维护入口放在底部。专题放在工作事项中，模块留在关联资源中。页面数量来自真实阅读任务，不因为多一个维度就增加一个菜单。

## 十一、原型已实现与产品仍待验证的分界

本轮 HTML 已提供真实可点击的界面和本地状态变化，关键交互已在 Chromium 中走查。但所有 AI、环境与业务数据均为模拟；真实同步、权限、冲突处理、代码与成果版本固定、远程状态一致性、运行恢复和真实能力评测尚未接入。

产品方向仍需重点检验：一项跨专题工作能否既表达不同目标又不重复汇报；需求级上下文能否减少补背景且避免持续膨胀；个人学习与爱好是否无需研发专属字段即可自然推进；从复盘到能力更新是否真正影响下一次执行。

不必重新选择 Agent 框架，也不要求先建设全能工作流编辑器。下一个有价值的推进单位是把一项真实需求接入现有 OpenCode 与工作站环境，验证共同上下文、委托、成果回传和人工判断之间的衔接。

## 参考材料与官方来源

### 用户材料

- 《团队协作工作台：产品定义与体验讨论稿.txt》：产品主张、事项协作空间、关系与视图、环境连续性等；本轮新增实体方案不等于原稿已批准。
- 《answer-from-knowledge.txt》：最低充分背景、不同问题精度、知识与当前事实源的边界。
- 《ingest-knowledge.txt》：知识候选、受审发布和代码变化回写。
- 《#-AGENTS.md-—-Omni-Brain-Harness-发布契约.txt》：轻重路径、局部请求不机械升级、能力范围以实际证据为准。
- 《05_sampling_rules.txt》：原型中采样与状态回查阅读样例的依据；不据此推断生产实现。
- 用户 2026-09-05 本轮消息：团队/个人环境和已选执行方案。

### 外部官方文档（查询日期 2026-09-05）

- [L1] Linear Concepts：<https://linear.app/docs/conceptual-model>
- [L2] Linear Projects：<https://linear.app/docs/projects>
- [L3] Linear Initiatives 与 Custom Views：<https://linear.app/docs/initiatives>；<https://linear.app/docs/custom-views>
- [L4] Linear Documents：<https://linear.app/docs/documents>
- [L5] Linear Assign and delegate issues：<https://linear.app/docs/assigning-issues>
- [L6] Linear Agent：<https://linear.app/docs/linear-agent>
- [L7] Linear Initiative and Project updates：<https://linear.app/docs/initiative-and-project-updates>
- [L8] Linear Coding sessions：<https://linear.app/docs/coding-sessions>
- [L9] Linear Peek preview：<https://linear.app/docs/peek>
- [R1] OpenCode Server：<https://opencode.ai/docs/server/>
- [O1] Codex Authentication：<https://developers.openai.com/codex/auth>
- [O2] Codex App Server：<https://developers.openai.com/codex/app-server>
