请在当前项目中完成一次真实的“代码变化 → 既有正式知识候选更新”。

现状：

- 当前仓库已经包含一套正式人工质检知识；它是父知识，不允许直接覆盖发布。
- 新的直接代码来源位于 `/home/yyh/project/qpl-evolution-import-v1`，固定 commit 为 `ea10860103a9d282dd05da3639f50f00cf45b882`。
- 这是现有 `Quality Platform Lab` 在验收分配缺口版本之后的顺序演进，不是新系统，也不是早期 Ratio 预览原型的一部分。
- 你可以使用当前项目的 AGENTS.md、Skills 和工具。不得读取 `/home/yyh/project/omni-brain/eval/`、任何 `eval/reference`、其他 Trial、`/home/yyh/project/omni-brain-snapshot-import-writeback-ref` 或其他模型候选。

用户希望最终从现有知识入口理解并使用新增的本地快照 CSV 导入能力：

1. 能从项目根完成预览和确认写入，并理解正常结果、拒绝结果和适用边界；
2. 能理解 CSV、领域对象、当前数据库行、差异计划和 PostgreSQL 写入之间的完整转换，不把实现压成一两句摘要；
3. 能明确哪些字段属于来源、哪些人工确认或执行状态必须保护，以及问题选项缺失时不能承诺什么；
4. 能理解文件身份、确认令牌、锁、事务、UPSERT 和幂等各自解决什么问题；
5. 能沿 CLI、模型、Service、Repository、数据库公共能力和快照表定位修改入口与依赖方向；
6. 能直接看到固定 PostgreSQL 中首写、重复导入、脏批、过期确认、状态保护和全量回归的代表性结果，而不是只看到“有报告”或“测试通过”；
7. 新事实要原位融入既有来源、系统、数据、软件架构、实现地图、公共能力和产品视图；既有分析功能、分配缺口、Ratio 原型、历史与生产未知不能丢失。

请自行按最低充分范围读取源码、测试、文档和固定运行证据。不要为了覆盖率通读 `node_modules`、`dist`、整个原料库或无关前端实现；固定提交已有范围充分的验证报告时优先核验并内化其具体结果，不机械重跑环境。

最终交给用户候选知识、产品视图和一份可读审查说明，明确哪些事实已确认、哪些仍未知。原始代码和正式知识必须保持不变，候选可审查、可拒绝、可回滚；未经人工批准不要发布。你无需等待本轮用户逐步批准，但必须停在待审查状态。
