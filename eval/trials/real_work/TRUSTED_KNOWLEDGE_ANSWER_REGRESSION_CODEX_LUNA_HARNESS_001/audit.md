# M2 可信知识问答：相邻回归首轮审计

## 当前结论

`621ddd3` 的三项通用修订产生了可观察收益，但问答能力仍未收敛。两个独立 Luna 会话都跳过了无关 `harness.yaml`、`docs/now.md` 和全库文件清单，输出统一使用绝对路径；Q5 完整列出五类对象，不再用“等”省略。内容为 **9/10**。

读取停止仍失败。Q5 一篇生命周期页已经逐字提供五类对象，模型又读数据流和总览；Q6 把 `knowledge/index.md` 内相对链接误当项目根路径，连续三次失败后批量读三篇规范页和两篇来源页。当前 M2 仍是高价值可试用能力，不是 verified。

## 冻结问题评分

| ID | 结果 | 判断 |
|---|---|---|
| Q5-01 | 通过 | 五项名称完整，没有“等”。 |
| Q5-02 | 通过 | 形成阶段和用途逐项明确。 |
| Q5-03 | 通过 | 每项不能替代的对象明确。 |
| Q5-04 | 通过 | 收束为抽样、判断、决定和实际状态不能混写。 |
| Q5-05 | 通过 | 使用绝对可点击路径。 |
| Q6-01 | 通过 | 明确两种分母未裁决，当前不能选边。 |
| Q6-02 | 通过 | 已完成口径评价已判断样本；已分配口径混入未完成进度。 |
| Q6-03 | **失败** | 说明两者不可共用“质量通过率”，但没有把 `已完成 / 已分配` 单列为验收完成进度公式。 |
| Q6-04 | 通过 | 业务规则 owner、当前决定和生产字段直接证据明确。 |
| Q6-05 | 通过 | 没有把历史阈值或目标版本冒充当前规则。 |

总分 **9/10**；Q6-03 是关键题，因此未满足“关键题全过”。

## 行为变化

| 行为 | 首轮答案包 | 本轮相邻回归 | 判断 |
|---|---|---|---|
| 启动读取 | 每题先读 Harness 状态 | 只读问答 Skill 后进入知识根 | 修复生效 |
| 有限集合 | 四子域被“等”省略 | 五对象完整列出 | 修复生效 |
| 引用 | 相对/绝对混用 | 两题均为绝对路径 | 修复生效 |
| 全库枚举 | 三题运行 `rg --files` | 无清单或宽泛搜索 | 修复生效 |
| 逐页停止 | Q4 超预算 | Q5/Q6 仍超预算 | 未修复 |
| 链接解析 | 未显性暴露 | Q6 三次漏加 `knowledge/` 前缀 | 新暴露通用缺口 |

Q5 输入 77,222 token，较内容相近的首轮 Q1 降低约 10%，但仍多读两篇。Q6 输入 122,208 token，其中三次失败和多读来源没有增加最终内容价值。

## 下一次 Harness 修改

继续只改问答 Skill 的通用动作：

1. 从最直接的一篇开始，每读完一篇就逐项检查原问题；只有明确存在未回答项才开下一篇；
2. “最多三篇”是硬上限而非建议目标，不预先批量读取三篇；
3. Markdown 链接相对于包含它的文档解析：`knowledge/index.md` 的 `domains/...` 实际是 `knowledge/domains/...`；
4. 规范页已经给出答案和补证责任时，不为增加引用数量继续读来源页。

随后使用新的算法实例题和空库运行判断题复核，不重跑 Q5/Q6。
