# 最终提交补充重放：运行失败证据

## 目的

在 Harness 发布提交 `ab902c6` 上补跑正向和不触发用例，排除发布元数据与负向任务身份边界
对已有正向能力造成回归。Prompt 与已通过 Trial 相同，模型仍为 OpenCode
`deepseek-v4-flash`（medium），参考答案和评分项未暴露。

## 结果

| 会话 | 运行到哪里 | 结果 |
|---|---|---|
| `ses_fff0e90aaffe0IpMDBXHIzImy9` | 正确完成身份对齐，读取软件参考、Diff、源码和验证记录 | 长时间无新输出，人工中断；没有候选文件 |
| `ses_fff07b5b5ffezngHX3HYaSr1EC` | 正确完成身份对齐，读取软件参考、固定版本、完整 Diff 和验证记录 | 长时间无新输出，人工中断；没有候选文件 |
| `ses_fff02bd10ffe1nwZXOuIRWBT8V` | 启动模型 | 会话无回答即结束；没有文件变化 |

这些运行没有产物，不能计为通过，也没有出现错误审查内容。由于内容合同的完整正向盲测已在
`ab40653` 形成 `17/18` 候选，而其后的 Skill 变化只增加任务错位阶段 A 边界，最终两次正向
重放也均正确进入原有对齐分支，本轮记为外部模型运行失败，不作为内容回归。完整会话导出保留
在本目录，后续模型服务稳定时可以用同一固定输入重放。
