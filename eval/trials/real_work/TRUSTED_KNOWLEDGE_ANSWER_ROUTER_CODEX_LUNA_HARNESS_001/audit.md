# M2 知识路由器集成：Luna 盲测审计

## 当前结论

Harness `0da62f3` 在两个此前未进入答案 Trial 的质检主题上取得 **10/10** 内容结果。Luna 两次都实际调用 `knowledge_route.py`，没有读取 Harness 状态、列目录、宽泛搜索、探测源码或修改文件；每题只读路由器给出的主入口和一个候补，命令均成功。

与路由器前的复杂问题相比，Q9/Q10 输入约 9.1—9.2 万 token，明显低于 Q8 的 15.6 万；页面从三篇降为两篇，搜索命令从一次宽泛 `rg` 降为零。Q9 的候补有明确必要性，Q10 的生命周期页内容虽相关但交付页本身已经足够，因此“最短停止”仍有一页可优化空间。

## 评分说明

冻结题初稿的 Q10-04 额外要求“REJECT 进入返工”，而用户 Prompt 只要求解释 PASS 为什么不等于交付完成。该隐藏要求超过用户问题，评分前已将本项收缩为 PASS 后的执行状态、可交付 Good 数量和交付目标；Trial 原始 Prompt、轨迹和答案均保留，可审计这一修正。没有把候选错误改写成通过。

## 内容结果

### Q9 人员历史与权限：5/5

- 同一 `employee_id` 多行、`(employee_id,id)`、`end_time IS NULL` 当前有效记录完整；
- 关闭旧行、插入新行、写 `t_personnel_op_log` 和同事务边界完整；
- 历史组归属不因调组回写，可解释旧快照；
- JWT `sub`、后端模块/角色/数据范围授权与前端显示裁剪完整；
- 标识冲突、SSO、Schema、迁移、服务和事务实现未知均保留。

### Q10 交付多状态：5/5

- 阶段、健康、行动项、验收/结论四轴完整，没有“等”；
- 四轴分别回答流程位置、风险、责任动作和质量判断；
- PASS、外部回查中、交付风险的并存因果清晰；
- PASS 后仍需执行、回查、可交付 Good 数量和交付目标；
- 交付中心、预测、提醒和完整生产闭环没有被误报为上线。

## 实际行为

| 场景 | 路由结果与读取 | 命令/失败 | 输入 token | 判断 |
|---|---|---:|---:|---|
| Q9 人员历史与权限 | 主入口数据流与状态；因角色权限未答，打开人员与权限候补 | 4/0 | 92,177 | 路由与停止通过 |
| Q10 交付多状态 | 主入口交付与行动项；再读生命周期候补 | 4/0 | 91,079 | 有界通过；第二页可省 |

两次输出都使用绝对可点击路径，候选仓零改动。模型没有看到用例、参考答案或确定性路由评测集。

## 成熟度判断

- `knowledge_route.py`：`Verified@quality-knowledge-ten-route-slice`；
- Skill + 路由器的实际调用：`Verified@quality-check-personnel-and-delivery-pair`；
- 可信状态表达：已有现状、冲突、算法、人员和交付多类内容证据；
- M2 整体：`Verified@quality-check-trusted-query-slices`，适用范围限于当前中文 Markdown 知识、Codex/Luna、单一质检领域和最多三篇规范页；
- 尚未验证：第二领域、OpenCode、英文/混合语言、大规模知识、精确源码联查和关系型多跳问题。

本轮不再为省掉 Q10 的一页继续改提示词或排序权重。下一条高价值主线应回到开发日常：把“可信任务上下文 → 真实代码修改 → 运行验证 → 知识回写”组合成一条端到端回归，或用第二领域反证 M2 路由泛化。
