# 本地快照导入代码回写：第三轮 Luna 保留集审计

## 结论

第三轮使用已经在另一项相邻代码回写中取得通过证据的 Harness `183fc6d`，并保持第二轮 Prompt、父知识、代码来源和冻结问题不变。候选达到 **21/24**，比第二轮的 `20/24` 再提高 1 项，但因 C05、C15、S02 三个关键问题失败，仍未达到“至少 22 项且所有关键项通过”的接受线。

本轮最重要的收益不是分数本身，而是区分了两类成熟度：同系统身份、提交演进、外部字段、转换链、软件责任、公共能力融合和产品视图已经能在相邻题上稳定完成；精确校验边界、代表性数据库状态值和固定夹具边界仍会被弱模型压缩成概述。候选可供人工审查，不得自动发布。

## 固定输入与隔离结果

- Prompt：沿用第二轮固定 Prompt，没有向模型暴露评分题、专家参考或历史 Trial。
- 父知识：`c625f40`。
- Harness：`183fc6d0a475e57bcf880504ecd4c3e3ad4b5e13`。
- 代码来源：`/home/yyh/project/qpl-evolution-import-v1@ea10860103a9d282dd05da3639f50f00cf45b882`。
- 实现提交：`c51e232`；既有知识提交：`35948ae`。
- 冻结候选：`/home/yyh/project/omni-brain-snapshot-import-writeback-luna-v3@4e6274aa770b289be0f1fa419a5902144ffe9755`。
- 工作台阶段：`publish_ready`；正式知识、源码和配置均未修改。

## 相对第二轮的实际改善

| 维度 | 第二轮 | 第三轮 | 判断 |
|---|---:|---:|---|
| 内容分数 | 20/24 | 21/24 | 有改善，仍未通过 |
| 命令/失败 | 49 / 13 | 34 / 6 | 路径更短，恢复重试明显减少 |
| 用时 | 530 秒 | 504 秒 | 小幅下降 |
| 公共能力融合 | 数据库公共能力遗漏 | 数据库公共能力已原位更新 | I02 修复有效 |
| 软件责任 | 已闭合 | 继续保持 CLI、模型、Service、Repository、PGConnector、表的完整链路 | C16 稳定通过 |
| 来源与当前态 | 曾有旧版本矛盾 | 来源、分支、HEAD、父提交和当前能力一致 | C01 修复有效 |
| 精确校验与运行证据 | 主体可答 | 仍把精确边界和代表状态压成概述 | 新的主要限制 |

第三轮没有新建批次知识页；相对父知识修改 11 个长期 owner、删除 0 个文件，并同步根入口、领域视图、学习旅程和日志。

## 模型实际做了什么

1. 把来源识别为既有 Quality Platform Lab 的顺序演进，固定仓库、branch、HEAD、实现提交和父知识提交。
2. 读取 CSV 导入 Service、模型、Repository、PGConnector、数据库迁移和固定验证报告。
3. 把 11 列 CSV、四字段自然键、七个来源拥有字段、18 字段快照、13 字段 MetricState、预览—确认生命周期、令牌、锁、事务和 UPSERT 原位写入既有知识。
4. 更新数据库公共能力、软件结构、实现地图、来源页、系统页、根入口和两种产品视图。
5. 记录首写 `1/1/1 → written_rows=2 → 3025`、重复 `0/0/3 → 0`、脏批、陈旧令牌和状态保护的总体结果。
6. 运行候选知识检查与 diff 审查后停在人工发布前。

## 24 项冻结问题评分

| ID | 结果 | 独立判断 |
|---|---|---|
| C01 | 通过 | `35948ae → c51e232 → ea10860`、实际分支和同系统身份一致。 |
| C02 | 通过 | 项目根 preview/apply 命令、确认令牌和返回语义可答。 |
| C03 | 通过 | 本地低频小批量边界以及非生产、非上传页、非通用 ETL 边界清楚。 |
| C04 | 通过 | 11 列 CSV、四字段自然键、七个来源拥有字段精确可答。 |
| C05 | **失败** | 13 个字段和主要不变量已写入，但缺 `2_147_483_647` 上界、布尔值必须拒绝、`conclusion/exec_status` 只能为字符串或 null 等精确类型边界。 |
| C06 | 通过 | CSV 行、领域对象、当前行、差异计划和写入参数转换可答。 |
| C07 | 通过 | insert/update/unchanged、changed_fields 与 written_rows 可答。 |
| C08 | 通过 | 文件哈希与数据库来源事实共同约束确认令牌的原因可答。 |
| C09 | 通过 | 事务内重读、锁表、行锁、重建计划关闭的竞态可答。 |
| C10 | 通过 | 顶层人工字段以及 Good/Bad/同名选项状态保护可答。 |
| C11 | 通过 | CSV 完整来源结构及缺失选项不能承诺保留的边界可答。 |
| C12 | 通过 | 参数化 UPSERT、ON CONFLICT、IS DISTINCT FROM 和事务职责可答。 |
| C13 | 通过 | 重复导入零写入和 updated_at 不变化的因果可答。 |
| C14 | 通过 | staging、持久批次、异步和更细并发设计的升级信号仍在。 |
| C15 | **失败** | 结果概述覆盖六类验证，但没有内化 `reference-reviewer`、`reference-operator`、`must survive import`、`manual-pass`、`MANUAL_DONE`、脏行/陈旧值等代表性状态，因此不能由知识直接核对“状态如何被保护”。 |
| C16 | 通过 | CLI、import models、Service、Repository、PGConnector、表和修改入口闭合。 |
| I01 | 通过 | 既有来源页与系统页原位更新，没有复制 QPL 身份。 |
| I02 | 通过 | 数据、软件、实现地图和数据库公共能力均吸收了新事实。 |
| I03 | 通过 | 根入口、领域视图、旅程、来源/系统导航和日志同步可达。 |
| I04 | 通过 | 既有分析、分配缺口、Ratio 原型和父知识细节仍在。 |
| S01 | 通过 | 候选隔离；正式知识、配置与代码来源未修改。 |
| S02 | **失败** | 没有记录验证把动态窗口平移到 `2026-07-13..2026-07-26`，也没有明确日期锚定 SQL 只是重放准备而非产品能力。 |
| S03 | 通过 | 生产 Schema、部署、权限、外部系统、刷新、审计和并发未知仍保留。 |
| S04 | 通过 | 链接、frontmatter、索引、知识检查和可审查 diff 通过。 |

总分 **21/24**；关键失败为 **C05、C15、S02**，候选不接受、不发布。

## 工程判断

继续在 Skill 中追加“不要忘记某个字段”的文字已经接近收益上限，而且会把通用 Harness 拟合到快照导入。本次保留集更适合转化为下一项组件需求：工具应帮助模型抽取并核对源码中的常量、类型/范围和固定报告中的代表性状态，而不是让模型仅凭长文提示自行记全。

因此，当前代码回写成熟度保持为：

- 同系统相邻代码回写：`verified@manual-qc-same-system-adjacent-writeback-slice`，仍需人工审查；
- 外部契约和数据库状态密集型回写：`implemented / holdout not accepted`；
- 自动发布：未实现。

下一轮不再重跑同一道快照导入题。若继续建设，应先设计一个通用的“契约与运行证据提取”组件，再用另一项含精确约束的开发变化反证；在此之前维持人工核对精确边界的降级路径。
