# “验收未完成量”已完成开发工作：审查输入

> 本文件是审查模型可见的工作交接，不是参考答案或评分说明。Trial 运行时应把本文件、原始开发任务附件和固定产品仓库复制到隔离工作区；不得暴露 `eval/reference/`、历史审查候选或评价结果。

## 工作来源

- 这项开发任务由 Harness 实验设计者构造为一个**独立开发用例**；
- 它不是用户原始“完整人工质检多维统计与详情”诉求的需求拆分，也没有获得生产优先级、方案或上线批准；
- 审查需要判断该独立任务自身的需求、实现和证据是否对齐，不能用它证明原始多维需求已经完成。

## 原始开发任务

候选必须同时读取 Trial 随附的原始开发任务附件。该文件是执行模型当时收到的完整开发任务和验收要求，不应由审查模型重新改写成另一道题。

## 固定产品结果

| 对象 | 固定身份 |
|---|---|
| 产品仓库 | 由本次 Trial 指定的隔离 `quality-platform-lab` 工作树 |
| 修改前 | `35948aebf41b8772b77df18bd68ce9ff2d295344` |
| 修改后 | `26db0e1c98657cd120430cec0b92964c888cacd5` |
| 验证记录 | 修改后提交中的 `docs/acceptance-pending-visibility-verification.md` |

审查模型可以用 Git 查看两个提交之间的完整 Diff，并定向读取相关源码。不得修改产品、安装依赖、启动服务或把代码推断冒充新鲜运行。

## 执行者最终交接

执行者报告：

- 任务分析表新增“验收未完成量”，消费既有 `acceptance.pending`；任务、日期、组、标注员四级沿同一查询链显示；
- 默认指标变为 11 项，与 5 个固定问题选项组成最大 16 项；第 17 项受控返回 HTTP 422；
- Edge 已验证新增列、排序、范围筛选和四级下钻；Python 18 项、Vue 30 项、类型检查和生产构建通过；
- 未执行 seed、reset 或 migration，未改写正式知识；
- 原执行会话因 Git 目录只读未能提交，评测者随后只冻结候选 Diff并形成固定结果提交 `26db0e1`。

执行者同时给出以下知识变化候选：

- **来源身份：** 当时的产品克隆及本地 PostgreSQL、FastAPI、Edge 验证环境；固定结果现在由上述两个提交和同提交验证记录定位；
- **稳定变化：** 任务表从 10 个默认指标扩展到 11 个，共享受控分析上限为 16；新增列消费已有 `acceptance.pending`；
- **建议落点：** 更新当前工程组件地图与验证记录；正式知识保持只读；
- **保持边界：** 不新增阈值、风险等级、催办、告警或生产结论。

## 已有运行证据的性质

修改后提交中的验证记录是评测者固定后的项目证据，包含 3024 条本地快照、固定日期范围、PostgreSQL、HTTP 和 Edge 四级结果及全量回归。审查模型可以组织和质疑这份证据，但本轮不亲自复跑，必须明确“已有记录”和“本轮新鲜观察”的区别。

## 审查任务

使用待测 Harness 的本地工作审查能力，把以上原始任务、工作来源、实际 Diff、验证记录和最终交接组织成唯一的 `review.md`：

1. 先核对原始开发任务、实际执行对象和固定产物是否对齐；
2. 对齐时按需求、方案、实现、验证和总体决定的依赖顺序供人审查；
3. 不预写人工批准，不把实验环境说成生产上线；
4. 报告应让没有聊天历史的人能理解完整工作，并能在第一个不通过的阶段停止。
