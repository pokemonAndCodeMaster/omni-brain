# 审查单：验收分配缺口详情（manual-qc-allocation-gap-detail）

> 状态：**任务对象错位，停止下游审查**。本审查单只做任务对象核对，不进入需求/方案/实现/验证审查，也不要求继续批准。

## 0. 结论先行：任务对象错位

三者不一致，冻结实验题**不是**用户原始产品诉求的合法切片：

| 对象 | 内容 | 范围 |
|---|---|---|
| ① 用户原始产品诉求 | 建设**完整、可复用**的质检标注与验收统计/详情能力：公共多维聚合展开表格（维度不写死，任选聚合维后沿剩余维展开）；**每个合法统计单元格**都可作详情入口继续按剩余维度分析；详情复用统计卡片+图表+勾选编辑交互；后端按查询效率/结果复用/使用频率设计接口；Good/Bad 分别观察验收通过率，整体完成率不拆分；公共组件不足时允许合理增强 | 一类产品能力，任何指标、任何维度、任何单元格 |
| ② 实际冻结任务（`production-prompt.md`） | 只把**"验收分配缺口量"一个单元格**变为详情入口，展示整体/Good/Bad 的预期/实际/缺口/达成率与按日趋势，复用现有四级 Scope、抽屉、ECharts、表格 | 单指标 × 单入口 × 已有下钻链 |
| ③ 实际产物（`544f06b`） | 缺口数字→键盘可操作详情按钮；整体/Good/Bad 12 项汇总+三条按日趋势；沿任务/日期/组/标注员复用现有 API/抽屉/图表；回归与键盘记录 | 即②的忠实实现 |

错位依据：

1. **范围收窄**：①要求"每个合法统计数值单元格都可成详情入口"，且详情按**剩余维度**继续分析；②只做一个"缺口量"指标，且不新增维度展开，只沿既有四级下钻对象展示。前者是可扩展产品能力，后者是单点功能。
2. **需求确认缺失**：本任务由 Harness 实验设计者后来构造，`review-input.md` 明确记录"没有找到用户批准该切片或方案的记录"；且 ① 原文明确"用户没有批准把它优先拆成'只解释验收分配缺口的 Good/Bad 构成与趋势'"——② 恰恰就是这个被明确否决的拆分方向。
3. ③ 完成得再干净，也只是证明 ② 这个独立实验题自身成立，不构成 ① 的任何阶段交付。

**停止规则（Skill 第 3 节）**：任务来源、实际任务与产物摘要已足以判定错位，不依赖任何实现事实，故不再继续读取 Diff 细节、源码与运行记录，也不撰写需求/方案/实现/验证审查。

## 1. 现有成果保留为独立实验事实（不删除、不追认）

- **身份**：产品仓库 `quality-platform-lab`（同级 `../product`）
  - 修改前：`35948aebf41b8772b77df18bd68ce9ff2d295344`
  - 修改后：`544f06bda7e35121fc53636655fd6b33618f106d`
- **Diff 入口**：`git -C ../product diff 35948aeb..544f06b`
- **执行者声明**（作为声明记录，未在本审查中复跑验证）：缺口单元格改为指标详情按钮；整体/Good/Bad 12 项汇总+三条按日趋势；沿任务/日期/组/标注员范围复用 API/抽屉/图表；PostgreSQL、HTTP、页面与全量回归通过；焦点进入、Tab/Shift+Tab 环绕、Escape 关闭和焦点恢复有记录；无新增数据库表、平行查询系统、问题选项缺口、阈值、负责人或自动动作。
- **边界**：以上证据只证明独立实验题完成情况，不自动证明用户原始产品诉求，也不因执行良好而解释成原始需求已完成。

## 2. 回到哪里

应回到 ① 的**需求与方案形成**，而不是本切片。回到该任务前需先回答：

- 公共多维聚合展开表格的整体范围与第一交付切片是什么；
- "每个合法统计单元格可作详情入口"的公共契约、入口生成规则与维度展开规则；
- 后端接口按查询效率/结果复用/使用频率的设计原则如何落地；
- 本次独立实验产物（单指标详情抽屉）是否作为 ① 的一个受控组成部分被复用或继续演进——这需要用户显式决定，不能默认并入。

## 3. 审查点（R1 即为阻断）

- **R1（已驳回）**：冻结实验题是否是用户原始诉求的合法切片？——否。依据见第 0 节。R1 不通过，后续 R2–R5 均不开放。
- R2–R5：**等待**。在 R1 对应的需求/方案问题回到 ① 并形成批准前，不要求审查方案、实现或验证。

## 4. 下一动作

- 用户：决定是否回到 ① 需求/方案形成路径（`task-knowledge-prep` / 方案形成），并显式声明独立实验产物如何处置。
- 本 Agent：在 ① 的方案经批准形成新任务后，若该任务包含对本次产物的复用或演进，再按新任务重开审查；在此之前保持本审查单为"任务对象错位、停止下游"状态，不推进 R2–R5。
