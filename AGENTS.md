# AGENTS.md — Omni-Brain 项目操作契约

本文件规定 Agent 在本仓库中的目标、判断顺序和安全边界。它不是完整产品规格，也不应复制仍在变化的设计细节。

## 使命与当前阶段

Omni-Brain 面向工作、开发和业务协作，目标是把分散在代码、文档、数据、流程和人员经验中的信息，转化为**人能理解、AI 能可靠调用、长期可维护**的知识系统。

近期优先验证：

1. 围绕真实业务开发任务组装可核验的设计上下文；
2. 让人能够浏览、学习、精确查询并逐层理解业务—系统—代码知识；
3. 让知识变更可追溯、可验证、可回滚，避免越维护越混乱。

当前仍处于**产品基调与信息架构发现阶段**。除非用户明确要求，不得把讨论假设当成定稿规格，也不得因为已有 stub、目录或 taxonomy 就反推产品设计已经确定。

项目入口：

- `docs/blueprint.md`：当前目标、架构、组件成熟度、焦点和路线的唯一总入口；
- `docs/product-direction-discussion.md`：产品判断的推理过程、争议、备选和未决问题；
- `docs/design-discussion.md`：早期调研、组件议题和历史上下文。

## 当前工作模型

不要再把“稳定骨架、预设视图、临时视图”理解成三套并列知识库。当前工作模型是：

```text
原始事实与资料
      ↓ 证据/定位
规范知识底座（唯一规范落点）
      ├─ 受维护的产品视图（可持久化、可治理）
      └─ 会话/任务投影（按需生成、默认不持久化）

治理控制面横切全部环节：
Schema / taxonomy / 状态 / 来源 / view registry / change set / eval
```

- **规范知识底座**：逻辑上保存稳定实体、关系、解释、适用范围、状态和证据；物理形式目前以 Markdown、源码/文档链接及可重建索引为主，但最终模型尚待实例验证。
- **受维护的产品视图**：面向浏览、学习、查询、任务等稳定工况的选择、排序、导航和展开规则；引用底座，不复制一套事实。
- **会话/任务投影**：针对一次具体意图组装的上下文或阅读路线；默认短生命周期，只有反复有用并通过验证后才能晋升为产品视图。
- **治理控制面**：约束底座和视图怎样创建、变更、验证、发布与回滚；它不是第四份知识内容。

这是责任和生命周期的分离，不是知识抽象层级。其有效性必须通过真实工作旅程验证。

## 决策成熟度

讨论内容按以下状态管理：

```text
open question → hypothesis → provisional → approved → implemented → verified
```

- `open question` / `hypothesis`：允许探索，不得写成硬规则；
- `provisional`：方向暂时成立，仍需实例验证；
- `approved`：用户明确认可，可成为规格或规范；
- `implemented`：已经落地但尚不能证明有效；
- `verified`：有真实任务、测试或运行数据支持。

任何从原则到机制的固化，都应能说明：解决什么问题、适用哪一层、如何执行、怎样失败、如何验收。高层口号不能直接升级成 Schema、Skill 或代码。

## Agent 的工作顺序

### 0.0 显式任务案恢复快速通道

用户明确要求恢复、查看或续接某个任务案时，这首先是**状态恢复**，不是新任务分诊，也不是按任务案名称查找同名 Skill。

- 已给出 `<case-id>`：直接读取权威状态 `workspaces/task-cases/<case-id>/case.yaml`。不得先列根目录、递归搜索、逐层遍历 `workspaces/`，也不需要先运行脚本；`overview.md` 和 outputs 是可重建视图，不能覆盖 `case.yaml` 的当前状态。
- 未给出 ID：先从 `docs/now.md` 的活跃线程定位；仍不明确时才运行 `python scripts/task_case.py list`，多个候选无法消歧时询问用户。
- 恢复只需先回答目标、焦点、阻塞项、门禁、最近动作和下一动作。只有回答依据不足时才渐进下钻：门禁原因读 `outputs/readiness-report.md`；问题答案只读其 `evidence_ids` 对应记录；历史原因只读相关或末尾事件；进入决策才读 `decision-context.md`；处理长期知识才读 `knowledge-proposals/`。
- 不为普通恢复读取全部 JSONL、全部 outputs 或 `scripts/task_case.py` 源码。仅恢复状态不依赖 Python 环境；后续操作工具因依赖不可用时报告阻塞并使用已声明的人工降级，不得自行安装依赖或切换、创建环境。
- 仅查看状态时不加载 `task-knowledge-prep`。用户要求继续推进且该任务仍属于 C 类时，再加载该 Skill 并从账本中的下一动作继续。

### 0. 任务路由与渐进式知识准备

每个任务先做一次低成本分诊：判断目标是否明确、直接事实源在哪里、未知信息是否会改变行动、失败影响与回滚成本，以及是否涉及跨域或长期决策。选择**最低充分**模式，不得把“任务通常需要背景”机械实现为所有任务都走完整流程。

- **A｜直接执行**：目标和验收明确、范围局部、风险低且容易回滚。只读取直接事实源和适用操作规则，不创建任务知识准备案，不做宽泛知识查询。
- **B｜有界上下文**：问题明确，但需要少量规范、历史决定或组件背景。使用 `knowledge-query` 或相关领域 Skill 做定向查询；默认不创建持久任务工作区。
- **C｜完整知识准备**：目标模糊、关键知识缺失/冲突/过时、跨业务—系统—代码、高风险难回滚，或会形成长期架构与公共能力。加载 `task-knowledge-prep`，迭代维护任务框架、问题/缺口和知识切片，通过决策准备与知识交接门禁。

默认从最低成本模式开始。出现范围扩大、关键来源冲突或缺失、高风险副作用、连续失败、无法解释方案依据等信号时，按 `A → B → C` 升级；目标收敛且知识已经充分时可以提前结束，不为完成流程而继续补知。知识范围以“是否会改变下一步行动、方案选择、验证、风险或结果解释”为准，而非收集所有相关材料。

`task-knowledge-prep` 当前是实验能力，允许用会话、文件工作区和人工检查建立可测基线；尚未实现的脚本或门禁必须显式标为组件缺口，不得把强模型临场补偿声称为 Omni-Brain 已验证能力。

### 0.1 组件进入默认工作方式的门槛

Blueprint 的设计成熟度不自动等于运行可用性。组件只有在实现并取得相应范围的任务/评测证据后，才升级 AGENTS.md 中的采用强度：

- `open / hypothesis / provisional`：仅实验或显式触发，不写成全局强制；
- `approved`：可以提供受控试用入口；
- `implemented`：可以使用，但必须说明尚未验证并保留降级路径；
- `verified@slice`：只在已验证范围内设为推荐或强制；
- 多场景 `verified`：才成为所有适用任务的默认能力。

组件回归、依赖不可用或超出验证范围时，降级到已声明的人工/直接事实源路径并记录失败。AGENTS.md 只保存稳定、跨任务的触发、强制/禁止事项和降级原则；详细步骤留在 Skill，设计理由进入 Spec，实验状态和指标进入任务案或 Eval。修改本文件时优先合并、替换或删除现有规则，不默认追加；单次案例、动态成熟度和仍在变化的命令不得固化为全局规则。重大修改应附代表性 Agent 路由回归。详细准入和容量治理见 [`docs/specs/agents-contract-governance.md`](docs/specs/agents-contract-governance.md)。

### 1. 先判断任务与事实源

- 产品定位、信息架构、当前进度与路线：先读 `docs/blueprint.md`，需要理由时再读讨论稿和相关决策记录；
- 知识库事实、历史规范、经验：查 `knowledge/index.md` 和相关卡片，并给出文件路径；
- 当前代码行为、接口、schema、配置：以源码、机器可读定义和运行结果为准；
- 外部技术、论文、产品现状：需要时查一手来源，不把调研结论伪装成本项目决定。

当前 `scripts/compile_index.py`、`search_engine.py`、`ingest_pipeline.py`、`health_checker.py` 是占位实现。可以执行以确认状态，但不得声称已经完成真实检索、索引、摄入或健康检查。

### 2. 先明确问题层，再提出改动

收到“不符合预期”“需要整理”“搜索不好”等反馈时，先区分：

- 内容事实问题；
- 关系、归属或知识粒度问题；
- 人类阅读视图问题；
- 检索与上下文组装问题；
- Schema、流程或治理规则问题。

修复最低必要层。内容正确但展示不合理时，优先调整视图，不移动或重写规范知识。

### 2.1 持续问题处理

用户提出日常困扰、反复难题或不友好体验时，先把它作为真实工作信号而不是立即当作需求、知识事实或架构结论。Agent 必须：

1. 还原目标、场景、当前做法、阻碍和影响；信息不足时明确待确认项；
2. 按本节的问题层分诊，并对照 `docs/blueprint.md` 判断：已被目标覆盖但尚未实现/验证、构成现有假设的反例，或暂不属于当前范围；
3. 给出最小行动及其验收方式：即时处理、待观察、候选 change set、用户旅程/评测用例，或升级讨论；
4. 仅在用户明确要求记录、沉淀或作为案例时保存原始材料；敏感信息默认不扩散；
5. 将可复用的结论按 `docs/specs/continuous-problem-loop.md` 分流。一次反馈不得直接改写 Blueprint、taxonomy、Schema 或规范知识。

持续问题处理协议是待实例验证的工作机制，不应在问题数量、字段和升级门槛尚未稳定前过早封装为 Skill。

### 2.2 对话结论回收与续接

项目讨论不以聊天记录本身作为长期记忆。每轮出现可复用的结论、取舍、反例、未决项、真实案例或验证结果时，Agent 应先给出“沉淀候选”：内容、依据、成熟度、拟关联对象和拟写入位置。经用户要求沉淀（例如“沉淀本轮”）后，按 [`docs/specs/conversation-knowledge-lifecycle.md`](docs/specs/conversation-knowledge-lifecycle.md) 分流；原始会话只在明确同意后新增至 `knowledge/raw/conversations/`。

对非简单的讨论与设计回复，Agent 还必须完成面向用户的行动交接：先给当前判断，结尾按需区分“本轮决定”和“现在怎么继续”。行动项必须明确推荐路径、用户需要做什么、Agent 将做什么和预期产物；若用户无需行动，应明确说明。不得假定用户理解“沉淀、问题记录、知识摄入、更新 Blueprint”等内部术语，首次用于当前情境时应说明触发后的实际结果。探索性回合可以只确认新发现、未决问题或下一步验证，不得为了格式制造虚假结论或行动。详细格式和例外见 [`docs/specs/conversation-knowledge-lifecycle.md`](docs/specs/conversation-knowledge-lifecycle.md)。

新一轮项目任务先读取 `docs/now.md` 的当前工作台，再以 Blueprint 确认当前目标、架构与路线；若问题涉及仍在验证的判断，还必须读取相关讨论稿、活跃问题案例、场景和最近验证结果。当前工作台只作续接视图，不能替代任何规范来源；不得把 Blueprint 未写入的内容视为不存在，也不得将未验证讨论直接写入 Blueprint。

### 3. 按任务类型行动

- **讨论/设计**：帮助用户从零散诉求中提取目标、场景、约束、矛盾、假设和验证方式；重要讨论重构进持续讨论稿，不在文末堆叠重复结论。
- **知识查询**：检索并引用具体文件；召回不足时明确知识缺口。
- **知识摄入**：保存不可变原料，区分候选知识与当前事实，完成冲突检查、人工门禁、索引登记和操作日志。
- **开发/排障**：先确认相关知识与源码现状，再做聚焦修改；用新鲜测试或端到端证据验证。
- **结构迁移**：使用独立的 brownfield 流程，在旧库之外建立受控层并逐片切换，不在混沌原库中直接大规模重写。

## 信息架构发现原则

现在可以进入“稳定知识骨架”的**发现阶段**，但不能直接定稿分类法、本体或物理目录。方法是：

1. 选择真实用户旅程和真实材料；
2. 列出完成任务需要回答的问题、事实源和决策点；
3. 找出跨场景稳定出现的实体、关系、证据和生命周期；
4. 形成最小候选骨架；
5. 用另一个领域/任务反证和修正；
6. 稳定后再升级为 taxonomy、Schema Pack、视图模板和检查器。

避免两种过早设计：从“工作”之类的抽象顶层向下穷举世界；从单个案例的目录结构直接推广成全局本体。

## 知识与代码边界

- 源码、接口定义、数据库 schema、配置和运行事实是实现真相；
- 知识底座维护模块职责、边界、关键流程、设计理由、影响关系和代码入口；
- 不为避免读源码而维护逐函数、逐行实现副本；
- 涉及精确当前行为或代码修改时，必须实时读取相关源文件/符号；
- 代码关联知识应可被变化触发复核，不能把旧摘要继续当成当前事实。

目标不是消灭源码阅读，而是把“通读陌生代码”缩小为“先获得地图，再精读相关实现”。

## 知识硬约束

- `knowledge/raw/` 中已有原料不可修改或覆盖；新原料只能新增；
- 关键结论必须能回到来源、源码或明确的人工决定；
- 同一核心定义应有唯一规范落点，视图和回答引用它而不是复制维护；
- 必须区分当前事实、历史决定、推断、待确认与失效内容；
- 新建知识卡必须符合当前 `knowledge/taxonomy.yaml`、登记到 `knowledge/index.md`，并追加 `knowledge/log.md`；
- taxonomy 与卡片类型是当前约束，不代表最终知识架构已定；重大调整必须提出迁移与回滚方案；
- 派生索引、关系图、缓存和视图输出必须可从规范来源重建。

## 安全变更协议

模糊反馈不得直接触发大范围覆盖式改写。涉及知识结构、Schema、核心视图或多文件知识的变更，先形成 change set：

1. 可复现的问题与期望结果；
2. 问题层和根因假设；
3. 修改前后差异与影响范围；
4. 结构校验、关键查询和用户旅程回归；
5. 人工确认点；
6. 版本记录、稳定标识/重定向和回滚方法。

优先局部、可逆、可验证的调整。失败时回滚整组变更，并把失败案例加入评测集；不要在失败结果上继续叠加补丁。

## 验证标准

- 设计结论：标注成熟度、依据、反例和待验证问题；
- 知识回答：给出具体文件路径和来源，区分事实与推断；
- 知识变更：检查完整性、来源、冲突、关系、索引登记和回滚能力；
- 代码变更：优先验证端到端用户路径，再补必要的单元测试；
- 视图变更：用对应的浏览、学习、查询或任务故事验证；
- 治理变更：比较调整前后结果，观察失败率、回滚率、问题复发和人工负担。

无法验证时，明确说明缺失的工具、数据或用例，不以“已执行流程”代替有效证据。

## 沟通与协作

- 面向用户优先使用中文；
- 先给判断和关键取舍，再给技术细节；
- 重要讨论的结尾给出可执行的行动交接；避免只有开放式建议而没有责任人、触发方式和预期结果；
- 不把推测包装成事实，不为了显得完整而虚构内容；
- 小而安全的改动可在说明假设后直接完成；会改变产品方向、知识语义或大量现有内容的事项先给出方案和影响；
- 保留用户已有修改，操作前查看 `git status --short`，不回滚无关文件；
- 搜索文件与文本优先使用 `rg`；
- 详细工作流可复用 `.agents/skills/`，但技能中的实现假设也是可演进资产，不自动等同于已批准的产品设计。

## 仓库导航

```text
docs/blueprint.md      当前项目架构、成熟度、焦点与路线总入口
docs/                 产品方向、场景、设计讨论与实施规格
knowledge/raw/        不可变原始资料
knowledge/            规范知识卡、目录、分类与日志
.agents/skills/       可复用 Agent 工作流
scripts/              检索、摄入、健康检查等确定性工具（当前多为 stub）
eval/                 固定问题、用户旅程与回归评测
src/                  产品实现
config/               配置
```
