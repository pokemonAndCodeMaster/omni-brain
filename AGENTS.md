# AGENTS.md — Omni-Brain Harness 实验契约

本项目是可由 OpenCode 从根目录直接使用的 Omni-Brain 精简 Harness。只声明已经实现或取得证据的能力；设计仓库、历史聊天、已删除旧知识和 stub 不自动成为本项目能力。

## 启动顺序

1. 读 `harness.yaml`，确认当前阶段、能力状态和禁止声明；
2. 读 `docs/now.md`，确认当前能力状态与用户任务边界；
3. 按任务路由加载一个最低充分 Skill；
4. 精确事实以源码、Schema、运行结果和直接人工确认为准。

## 任务路由

- 用户要求整理、导入、摄入或归并一批材料，建立或更新规范知识与产品视图：加载 `ingest-knowledge`。该 Skill 自带摄入工作台，不再叠加 `task-knowledge-prep`。
- 用户明确恢复、续接或查看任务案：按“显式任务案恢复”处理；继续 C 类任务时再加载 `task-knowledge-prep`。
- 目标明确、局部、低风险、易回滚：直接读取事实源后执行。
- 问题明确但需要少量背景：定向读取，不创建持久工作区。
- 目标模糊、关键知识缺失/冲突、跨域、高风险或形成长期能力，且不属于知识摄入：加载 `task-knowledge-prep`。

不要把所有任务机械升级为完整流程。

## 当前能力

- 任务案恢复、原子问题回答和机械门禁：已在有限切片验证；
- 本地 Git 来源范围与版本记录：已实现有限切片；
- 知识摄入 Skill、确定性摄入工作台、空 OKF Bundle、领域地图、产品视图入口和只读校验器：首轮真实 Trial 已暴露内容与执行缺口，修订版等待重跑；
- 正式领域知识、真实查询、图/向量索引、Web UI 和知识演进闭环：尚不存在。

不得把结构测试通过表述为知识正确或 M1 已验证。

## 知识摄入硬边界

- 正式知识只发布到 `knowledge/`；候选只写入 `workspaces/knowledge-ingestion/<case-id>/`。
- 来源保持只读并记录版本、实际读取范围、能证明和不能证明的事项。
- 用户批准前不得修改正式 `knowledge/` 或 `config/knowledge-domains.yaml`。
- 摄入案必须先由 `python scripts/ingestion_workspace.py init` 建立；初始化前不递归枚举来源，来源数量、逐文件身份和覆盖状态以机器账本为准，不手工猜测。
- 首批知识未同步形成领域位置视图和旅程/学习视图时，不得报告完成。
- 跨域关系使用带业务解释的标准 Markdown 相对链接；禁止 Obsidian `[[...]]`、`file://`、本机绝对路径和未经批准的固定关系词表。
- 公共能力只有在共同核心、领域差异、维护责任和人工批准都成立后才能抽取。
- 人工审查前必须运行 `python scripts/ingestion_workspace.py check <case-id>`，最终交接只给工作台根 `review.md`；发布后再运行 `python scripts/knowledge_check.py` 并展示正式知识 diff。检查失败则停止或回滚整组正式发布，保留工作台和失败证据。

详细步骤与模板只在 `ingest-knowledge` 中维护，本文件不复制。

## 显式任务案恢复

用户给出 `<case-id>` 时直接读取 `workspaces/task-cases/<case-id>/case.yaml`，不得先搜索同名 Skill、遍历目录、读取全部 JSONL 或安装依赖。

先只恢复目标、焦点、阻塞项、门禁、最近动作和下一动作。用户要求继续推进且任务属于 C 类时，才加载 `task-knowledge-prep`。

## 来源边界

- 只读取本项目和用户明确指定的材料位置；不要自行搜索同级项目、历史聊天、旧答案或其他未声明材料。
- 用户指定的来源保持只读。需要扩展读取范围时，先说明哪个关键问题因此无法回答，并取得用户同意。
- 项目内没有业务参考答案。不得把测试、模板或工具实现当作待摄入主题的事实来源。

## 验证与沟通

- 代码改动运行 `python -m unittest discover -s tests`；
- 知识结构运行 `python scripts/knowledge_check.py`；
- 内容质量通过真实浏览、真实问题、来源回查和用户确认验证。

优先使用中文。先给判断，再给证据与边界；结尾明确用户需要做什么、Agent 将做什么和下一产物。
