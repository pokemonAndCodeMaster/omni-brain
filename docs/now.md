# 当前能力

> 最近更新：2026-08-10。

## 当前状态

M1“知识整理与摄入”已完成质检增量文档和整库阅读整改两个真实切片：正式增量知识通过人工批准与跨模型重放；整库整改由 OpenCode 弱模型在隔离候选中自然运行完成，27 个用户页面全部审视且内容审计超过接受线。当前进入 M2“可信查询与任务上下文”，直接消费已发布知识。

同一个 `ingest-knowledge` Skill 提供三条最低充分路线：

- **宽范围完整整理：** 固定材料 → 可完整阅读的材料单元 → 带必要细节和精确定位的发现 → 长期知识主题 → 发现到正文标题映射 → 产品视图与来源追溯；
- **聚焦代码或问题：** 明确问题 → 小批直接来源 → 规范知识 → 需要时在隔离环境真实运行。
- **整库阅读审视：** 公开页面清单 → 独立读者问题 → 一至三页任务包 → 逐页修改或有理由保留 → 从根入口走通。

用户只需从项目根目录提出自然语言请求，最后审查候选知识、产品视图和 `review.md`。后台状态仍只有 `.state/case.json` 与 `.state/source-manifest.jsonl`。

已有正式知识时，工作台会先冻结父版本文件指纹并把它原样复制到候选；本次只扫描新增材料。知识主题显式区分新增、原位更新和合并，审查页展示父版本到候选的实际差异。未计划的正文/视图修改和父知识删除会阻止发布准备。

## 已有证据

- 不重复叙述文档独立审阅，发现保留必要细节和精确定位，并唯一归入一篇规范正文、映射到真实标题；
- 完整整理已在一批固定真实材料上形成规范知识、产品视图和可追溯来源，并达到预定内容接受线；
- 模型手写来源路径曾出现错误，现改为从已校验来源、发现和正文落点自动生成追溯视图；
- 回归、Skill 校验和候选领域地图下的知识检查通过；
- 原始材料保持不变；正式 `knowledge/` 与领域配置已经通过人工批准从候选发布。
- 父版本继承、原位融合、旧来源保留、入口语义同步和正式知识检查已经在真实增量材料上完成。
- 另一个独立模型能通过多个新会话依靠工作台游标继续同一长任务，最终达到发布准备；内容、安全和父知识保留达到相同质量线。
- OpenCode 弱模型在不阅读内部状态和工作台源码的情况下，用小批页面完成整库阅读整改；入口、领域全貌、业务主线、软件讲解和待确认事项都产生可用内容。

这些证据只证明当前质检增量和整库阅读切片有效。不能据此宣称跨领域、任意规模或所有 OpenCode 模型已经验证。

## 当前边界

- 读后发现、知识归属和正文表达仍由模型完成；工具只校验来源、顺序、一致性和可重建关系，不判断业务真伪；
- 逐文档审阅提高内容保真，也增加了命令与阅读成本，仍需在后续用例观察人工与模型负担；
- Python 包、模块、函数和方法细节在主题合并时仍可能被压缩；
- 缺少直接证据的 OBS、真实 Delta、生产状态和规则边界只能保留为未知；
- 领域配置已随正式知识一起人工审查；后续调整仍不能自动修改正式分类；
- 增量新增、原位修改、冲突/未知保留和产品视图同步已有跨模型内容证据；
- 消费会话仍会过读知识页和项目状态文件；正式查询、Web 界面和知识随代码变化自动演进尚未实现。

## 下一验证动作

1. 从正式 `knowledge/index.md` 上的真实问题建立直接 Markdown 查询基线；
2. 比较答案正确性、未知/冲突、漏召回、无效阅读和停止条件；
3. 确认 M2 用户流程、信息结构、技术选项和通过标准后，再实现最薄查询/上下文能力。

不要为同一固定材料继续增加 M1 Prompt 条款或后台字段，也不要在查询失败证据出现前预设向量库、图数据库或新的 Agent runtime。
