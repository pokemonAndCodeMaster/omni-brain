# 验收未完成量增量知识回写参考候选

## 当前结论

专家参考候选位于 `/home/yyh/project/omni-brain-pending-writeback-ref` 的 `reference/m4-pending-visibility-writeback-v1@760a86d6cf982f8f5358b30d9fba78ebeae1ae7a`。它从父知识候选 `28b0951` 开始，原位修改 9 个既有知识与产品视图文件，新增 51 行、删除 22 行；没有新增“本次开发”知识页，也没有发布到正式知识仓。

这是用于比较内容与融合质量的**强参考答案**，不是要求 Trial 复制文件数量、章节标题或措辞的模板。

## 参考结构取舍

- 更新既有来源页与系统页，将当前版本固定为 `26db0e1`，并保留父实现 `35948ae`；
- 在系统页加入用户能力、业务语义、消费链和真实运行证据；
- 在数据页区分聚合后的验收未完成量与分类原子分配缺口；
- 在软件结构页解释这是“后端已有、前端新增消费”的变化类型，并更新修改入口与组合上限；
- 在实现地图和公共工作台页补充新消费者、旧配置兼容与样式边界；
- 同步根入口、学习旅程和日志，不创建按 commit 或批次组织的平行导航。

## 关键真值

- `acceptance.pending = max(sum(actual_alloc) - sum(actual_complete), 0)`；
- 它表示已分配但未完成的验收工作，不等于分配计划缺口，也不是未经确认的风险阈值；
- 后端 Catalog 与 Repository 公式在父版本已经存在，本次贯通请求、类型、映射和表格列；
- 任务表当前为 11 个基础指标加最多 5 个问题选项，16 个通过、第 17 个受控拒绝；
- 完整 14 天现场的任务—日期—组—标注员代表链为 `1737 → 144 → 51 → 20`；
- 18 项 Python、30 项 Vue、类型检查、生产构建、真实 API 和 Edge 行为均有固定报告；
- 当前仍只是本地只读分析实验，不能外推生产部署或执行闭环。

## 参考验证

- `python scripts/knowledge_check.py`：36 个 Markdown、27 个概念，PASS；
- `git diff --check`：PASS；
- 父知识候选、来源代码和正式知识仓均未修改。

## Trial 重点

1. 能否先识别同一系统更新，并避免重新摄入整个代码仓；
2. 能否看出公式已存在，只补消费链，而不是机械复制上一项指标的改法；
3. 能否把内容原位融入多个长期 owner，同时保持父知识；
4. 能否保留真实运行的范围与代表值，并避免把红色样式或正数推断成风险；
5. 工作台和 Skill 是否真正降低了弱模型的搜索、遗漏和无序修改成本。
