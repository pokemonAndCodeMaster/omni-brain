# 用户请求

我们刚在 `/home/yyh/project/qpl-pending-ref` 完成并验证了人工质检任务分析表的“验收未完成量”展示，固定提交是 `26db0e1c98657cd120430cec0b92964c888cacd5`，父实现是 `35948aebf41b8772b77df18bd68ce9ff2d295344`。验证报告在该仓库的 `docs/acceptance-pending-visibility-verification.md`。

请把这项代码变化回写进当前项目已有的质检知识，使不了解本次开发的人能够：

1. 理解验收未完成量回答什么业务问题，以及它和分配缺口为什么不同；
2. 找到从后端指标到 Vue 任务表的实际消费链和以后修改的入口；
3. 知道当前能力真实验证到了哪里、哪些内容仍不能外推到生产；
4. 从既有知识入口和验收学习路线直接找到这些内容。

请使用项目现有 Harness。来源代码和既有知识都不能直接覆盖；先形成隔离候选供我审查，未经批准不要发布。不要读取任何 `eval/reference/` 或历史 Trial。
