# 当前实验工作台

> 最近更新：2026-07-22。

## 当前状态

M1 首轮 Luna 真实 Trial 已到人工门禁：安全隔离和知识 Bundle 检查生效，但来源计数、工作台入口、交接链接、内容深度和读取成本没有通过。当前修订版状态是 `implemented_after_first_trial_awaiting_retest`，正式知识仍为空，也没有参考答案泄漏到本项目。

## 当前可用

- OpenCode 原生根路由；
- `ingest-knowledge` 的盘点、候选编织、人工门禁、发布与立即消费流程；
- `ingestion_workspace.py` 的机器来源清单、逐文件覆盖账本、固定根入口、内容完成契约和工作台门禁；
- 空 OKF Bundle、领域地图和两类产品视图入口；
- 标准 Markdown 相对链接约定与只读链接/入链检查；
- 已有任务案和本地 Git 来源范围能力。

## 当前未证明

- 修订后的 Harness 能否让 Luna 稳定遵循流程并降低人工纠正；
- 真实材料能否形成高质量规范知识与产品视图；
- 算法细节、现实形态、冲突和未知能否保持准确；
- 用户审查负担是否可接受。

## 下一动作

从干净基线启动新的 Luna 会话，只提供与首轮相同的一条真实摄入需求。比较来源事实错误、内容覆盖、工作台门禁、人工负担和 token 成本；在结果通过人工审查前不得升级能力状态。
