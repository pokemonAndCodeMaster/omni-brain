# 当前实验工作台

> 最近更新：2026-07-25。

## 当前状态

知识摄入已能在发布前隔离候选并形成局部业务—代码纵切。V9 前向 Trial 能形成来源主题地图、三层定位和 PostgreSQL 能力页，但弱模型在长文只展示前 24 行后仍会声明 `read_full`，内容范围没有增长。当前第九版状态是 `implemented_content_first_v9_forward_test_failed`；正式知识仍为空，也没有参考答案或业务评分点进入本项目。

## 当前可用

- OpenCode 原生根路由；
- `ingest-knowledge` 的盘点、候选编织、人工门禁、发布与立即消费流程；
- 面向弱模型的内容主线：先定义读者问题和“上级定位 → 主题全貌 → 用户焦点”，再匹配直接事实源、设计知识地图、从全貌下钻到代码/算法，最后从产品视图实际回答问题；
- `reader-answers.md` 逐题消费页：每个核心问题都必须给出可靠答案、部分答案、明确未知或确实不适用，并限制到产品视图和最多三篇规范页；
- `ingestion_workspace.py` 的机器来源清单、Agent 覆盖声明、固定根入口、内容声明契约和结构门禁；
- 空 OKF Bundle、领域地图和两类产品视图入口；
- 标准 Markdown 相对链接约定与只读链接/入链检查；
- 已有任务案和本地 Git 来源范围能力。

## 当前未证明

- 是否应在现有工作台工具中增加有界分块读取和机器 `displayed_complete` 事实；
- 若增加，能否只约束“完整展示”而不把它误称为模型已经理解；
- 有真实完整展示事实后，来源主题复核能否把快照、Delta、结论执行等材料带回正文；
- 后置消费者能否按上级、主题全貌和用户焦点的原始问题范围判断完整性；
- Luna 能否为当前业务与软件结论选择匹配的直接事实源，并沿真实动作下钻 Python 实现；
- 真实材料能否形成高质量规范知识与产品视图；
- 算法细节、现实形态、冲突和未知能否保持准确；
- 用户审查负担是否可接受。

## 下一动作

等待用户确认下一技术切片。推荐在现有 `ingestion_workspace.py` 内增加有界来源读取：每次输出受控分块并记录范围，全部内容实际展示后才写机器 `displayed_complete=true`；`read_full` 必须以该事实为前提。它不判断材料相关性或模型理解，也不引入服务、数据库或独立控制面。
