# 来源纠偏后的纵向切片验证记录

## 验证对象

本记录验证的不是静态页面，而是以下真实数据链：

```text
YAML / .env
→ ConfigManager
→ 本地 PostgreSQL 16.14
→ SnapshotRepository / Service
→ FastAPI
→ Vue 人工质检快照页
```

验证日期为 2026-07-26。运行数据位于 `.runtime/`，不进入 Git。

## 结果

| 验证项 | 实际结果 | 状态 |
|---|---|---|
| Python 自动化测试 | ConfigManager、分析目录与查询校验、来源约束、视图配置 Schema、HTTP 参数边界归一化等共 16 项通过 | 通过 |
| PostgreSQL 初始化 | 当前用户独立集群，Unix socket 可连接 | 通过 |
| 来源目录对齐 | 后端采用 `src/api|config|database|manual_qc`，前端位于 `src/frontend` | 通过 |
| migration / seed | 18 字段 JSONB 快照结构写入 14 天、24 个任务、12 个组、120 名轮换标注员，共 3024 条员工日快照；选项保留问题标签层 | 通过 |
| 数据库字段核对 | 18 个顶层字段顺序与 `V20260709_01` 显式清单一致 | 通过 |
| 确定性 SQL 核对 | 3024 条员工日快照、28 条日期—项目、336 条日期—任务；指定任务得到 42 条日期—组和 42 条日期—组—员工；并核对低完成、低通过、零分母、覆盖率超过 100% 和提交未分配边界 | 通过 |
| 字段业务语义 | `scene_name` 为标注任务/批次；`project_name` 只使用 `园区`、`城区/高速` | 通过 |
| FastAPI | health、rows、project/scene/group/employee 聚合可通过 HTTP 查询；rows 返回问题标签→选项→指标 | 通过 |
| Vue 组件回归 | 表格、范围筛选、完整分页取数、指标聚合、可组合总览、多图层图表、四级任务表、详情侧栏与页面组合共 29 项通过 | 通过 |
| Vue 类型与构建 | `vue-tsc` 通过，Vite 生产构建通过 | 通过（有 bundle 大小警告） |
| HTTP 数据链 | health、rows、scene 聚合和前端路由均可真实访问 | 通过 |
| V2 指标目录与任务查询 | `GET /api/manual-qc/analysis/catalog` 返回 5 个基础维度和 37 个统计指标；`POST /query` 在服务端完成任务跨日期聚合、比例筛选和排序 | 通过 |
| V2 动态问题选项 | `POST /facets` 返回问题标签和选项；指定 `驾驶行为分类 / CUT_IN` 后可按“占 Bad 比例 > 20%”筛出唯一任务 | 通过 |
| V2 任务根表 | 3024 条员工日快照在服务端汇总为 24 个跨周期任务；页面组件按下钻路径、标注、验收进度、验收结果分组展示 | 通过（组件测试、生产构建与真实 Edge 路由渲染） |
| V2 四级下钻 | 默认 24 个任务；展开“城区交互任务-01”得到 14 个日期，展开 `2026-07-13` 得到 3 个组，展开“质检组-01”得到 E003、E006、E009 三名标注员 | 通过（真实 Edge 逐级点击与真实 API） |
| V2 指标详情 | “拥堵跟车任务-08”完成率详情返回分配 2674、完成 937、未完成 1737、整体完成率 35.0%、Good 40.6%、Bad 30.0%，并展示 14 天趋势和 8 个问题选项 | 通过（真实 Edge） |
| V2 动态问题选项列 | 固定“驾驶行为分类 / YIELD”的完成率后生成可筛选、可排序新列；最大值 20% 的范围筛选把 24 个任务缩为 23 个；保存刷新后准确恢复 | 通过（真实 Edge，验证后清理测试配置） |
| V2 表头与筛选交互 | 四个默认业务分组分别跨 4、2、4、1 个实际子列；任务和项目同时支持文字包含及精确多选；筛选层挂载到页面根节点，数值范围筛选完整可用 | 通过（组件测试与真实 Edge 交互） |
| V2 单列调宽 | 在真实 Edge 中把“标注任务 / 下钻对象”从 230px 拖到 290px，其余 11 个叶子列宽度变化均为 0 | 通过（组件测试与真实 Edge 指标） |
| V2 层级筛选与下钻 | 精确筛选“城区交互任务-01”后仍能展开 14 个日期；对象类型选择“组”时保留任务、日期路径，继续展开得到 3 个组且不混入标注员 | 通过（组件测试与真实 Edge 逐级点击） |
| V2 日期区间筛选 | 统计日期列使用两个原生日期输入；起止均为 2026-07-20 后，展开任务只显示该日期 | 通过（组件测试与真实 Edge 交互） |
| V2 页面空白回归 | 真实路由显示“按标注任务定位到日期、组和标注员”及 24 条任务，无未处理 Vue 渲染异常 | 通过 |
| 规模数据完整性 | V1 总览经分页取齐 3024 行后显示总标注 248810、Good 179883、Bad 68927，与 PostgreSQL 全表聚合一致；超过 10000 行时明确拒绝部分统计 | 通过（真实 Edge、SQL 与前端测试） |
| Windows 最小化回弹隔离 | 用户现有 Edge 窗口仍可稳定复现；同一真实路由在独立用户目录、禁用扩展的 Edge 窗口最小化后连续 20 次采样均保持最小化；源码无主动聚焦/恢复窗口逻辑 | 页面代码侧未复现，现有 Edge 环境待排查 |
| Windows Edge 页面渲染 | Windows Edge 真实路由能恢复已保存卡片；当前项目为 2 个、标注任务为 24 个，编辑器能读取已保存标题、数据定义和样式 | 通过 |
| 卡片拖动与缩放 | 在真实浏览器中拖动后 `x: 0 → 3`，缩放后 `w: 6 → 7`、`h: 6 → 7`，页面进入未保存状态；增高一格时卡片内容区、图表布局区、ECharts 容器与 canvas 同步增加 58px，来源栏高度不变 | 通过（真实 Chromium 尺寸测量） |
| 卡片编辑与恢复 | 在真实 Edge DOM 中把标题改为“卡片编辑与保存验证”，保存并刷新后准确恢复，再还原原标题并保存 | 通过 |
| 卡片保存与恢复 | 保存后刷新页面，布局、查询和样式从 `t_portal_view_config` 恢复；统计值按最新快照重新计算 | 通过 |
| 业务总览数据 | 真实 Edge 中全量标注 248810，拆为城区/高速 130040、园区 118770；Good/Bad 和比率同步按项目展示，并与 PostgreSQL 聚合一致 | 通过 |
| 业务总览编辑与跳转 | 修改标题和重点色后保存，API 版本更新且刷新准确恢复；还原后保存；点击卡片定位到对应固定统计 | 通过 |
| 总览卡片 V2 迁移 | 3 张 V1 固定总览自动迁移为 `dashboard-v2` 指标块和项目拆分块；保存后 PostgreSQL 只保留定义，刷新用当前 3024 条快照重算 | 通过（Schema 测试、真实 API 与 Edge） |
| 总览内容块编辑 | 复制标注卡后新增说明块，把拆分从项目改为组；页面立即显示 8 个质检组，保存刷新后说明与拆分定义完整恢复 | 通过（真实 Edge） |
| 总览复制与恢复 | 复制品使用独立用户来源；原系统卡临时修改标题后恢复为当前预设，复制品不受影响；验证后删除副本并保存 | 通过（纯逻辑测试与真实 Edge） |
| 高密度图表与趋势 | 标注图同时含 Good、Top Bad 选项、其他 Bad、Good/Bad 比率；三张固定图均可切换按天趋势；24 个任务时使用短标签，默认 10 项窗口可横向缩放和平移，横轴不再重叠 | 通过（真实 Edge 截图复核） |
| 多图层统计图 V2 | 4 张固定统计迁移为系统预设；同一卡片可组合柱形、折线、面积、双轴、拆分和图层独立筛选；主要聚合由受控分析接口完成 | 通过（Schema、组件测试与真实 Chromium） |
| 图表实时预览 | 把验收进度横轴改为日期、分配量按项目拆分、新增园区通过率面积图后，预览返回 14 个日期和 5 条序列；配置区滚动 700px 后右侧图与聚合数据表仍保持可见 | 通过（真实 Chromium DOM 指标与截图） |
| 图表保存、复制和恢复 | 应用 5 序列定义并保存，刷新完整恢复；复制产生独立用户卡片；恢复系统预设回到 3 图层任务视图；删除副本后保存为 5 张 V2 卡片 | 通过（真实 API、PostgreSQL 配置与 Chromium） |
| 图表配置迁移与校验 | V1 维度、指标、组合样式和布局迁移为普通 V2 图层；后端严格校验坐标轴和图层引用；最终配置没有遗留 `query/style` | 通过（Python/Vitest 与真实保存结果） |
| V2 收束回归 | 删除无运行时引用的 V1 固定图和日期—项目表；保留仍承担迁移/回退职责的配置与旧 API。重载后仍有 5 张图表、24 个任务，首个任务展开得到 14 个日期；图表预览正常且控制台无异常 | 通过（引用审计、生产构建与真实 Chromium） |
| Bad 问题定位 | 真实数据展示驾驶行为分类/MERGE、CUT_IN、YIELD 和障碍物类型/STATIC 的数量与占比排行 | 通过 |
| 表格比率筛选 | 完成率最大值 86% 后，4 条项目日聚合正确缩为 2 条；数值聚合条件申请全页时明确保留在明细 | 通过 |
| 表格倒排 | 标注提交首次点击即降序，首行从自然顺序切换为 385 条的城区/高速项目 | 通过 |
| 自定义双轴图 | 真实生成按天“验收分配、验收完成、完成率”卡片，数据为 140/121/86.4% 与 146/127/87%；保存 API 成功，验证后清理临时卡片 | 通过 |
| Navicat TCP 路径 | 使用 `127.0.0.1:55432`、数据库/用户 `quality_lab` 和本轮重置密码完成 PostgreSQL 认证，服务端返回 IPv4 地址、端口、数据库和用户 | 后端通过，待用户在 Navicat 复核 |

## 实际命令

```bash
scripts/postgres.sh init
.venv/bin/python -m src.cli migrate
.venv/bin/python -m src.cli seed
.venv/bin/python -m src.cli health
.venv/bin/python -m src.cli verify
.venv/bin/pytest

cd src/frontend
npm test
npm run type-check
npm run build
```

## 当前能力边界

本轮已经完成“人工质检快照只读链 + 可保存业务总览与统计看板 + 任务优先分析明细”：
能够先看全量和项目拆分的标注，再看验收分配、完成、通过和打回；任务根表可逐级下钻
到日期、组和标注员，Good 占比、完成率和通过率可进入整体、Good/Bad、趋势和问题选项
详情。关注的问题选项可固定为可筛选、可排序的新列；列显隐、顺序、宽度和固定选项可
保存，一次性筛选与展开不持久化。总览与统计卡片仍可编辑、拖动、缩放和保存。

平台还不包含采样配额、精确任务预览、人工确认、执行、部分失败和状态回查；
总览和统计图已经使用 V2 配置模型，不再挂载 V1 固定图或日期—项目表；仍未实现
验收覆盖矩阵、预期/实际配额对照，以及采样—确认—执行—回查的操作闭环。
V1 配置迁移与旧四级 API 仍作为有意保留的回退边界。这些边界与后续选择见
`docs/manual-qc-analysis-workbench-v2-design.md`，不能把当前结果描述为
完整人工质检平台。
