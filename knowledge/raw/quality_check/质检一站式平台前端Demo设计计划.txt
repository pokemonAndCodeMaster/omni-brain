---
id: "SYN-QC-FE-DEMO-PLAN-001"
title: "质检一站式平台前端Demo设计计划"
domain: ["common_infra"]
type: "synthesis"
tags: ["质检平台", "前端Demo", "交互原型", "HTML", "设计计划"]
created: 2026-07-04
updated: 2026-07-05
sources: 12
status: active
related_code: ["demo/quality-check-platform.html"]
affects_path: []
trigger_keywords: ["质检前端Demo", "HTML原型", "信息架构", "交互计划"]
summary: "质检一站式平台可离线 HTML Demo 的设计计划：Demo 目标、五块页面范围（业务总览/人工/自动化/大模型/专题）、必操用户路径、共享组件与状态清单、视觉方向草案、设计拨盘与实现约束、P0–P4 分阶段交付、当前实现状态、已冻结 Demo 假设与本轮重设计范围。被顶层架构卡 §0「前端 Demo 入口」引用。"
---

# 质检一站式平台前端 Demo 设计计划

← 平台边界：[[质检一站式平台顶层架构]]。模块事实源：[[质检一站式平台人工质检模块整体架构]]、[[质检一站式平台大模型质检模块整体架构]]。

全局视觉、可访问性和动效硬规则见 [[质检一站式平台前端Demo设计规范]]。

> 📌 **源仓库吸纳（2026-07-06）**：本卡由源仓库 `ai-knowledge-base` commit `3cf39347` 的同名卡吸纳新建。原内容完整保留，frontmatter 按本项目规范补全 `id` 与 `summary`。本卡是 [[质检一站式平台顶层架构]] §0「前端 Demo 入口」引用的实体卡。

## Demo 目标

用一个可离线打开的 HTML 原型证明统一平台壳层能承载两个差异明显的样板模块，并完整演示关键业务交互。它不是静态大屏，也不假装已接后端。

默认桌面优先，同时保证窄屏可浏览。首屏确定为 [[质检业务总览信息架构]]；点击近期交付、业务摘要、里程碑或风险后进入带筛选上下文的模块总览。

## 页面范围

1. **质检业务总览**：里程碑与阶段目标置顶；近 1–2 周质量护航台账分开人工/抽检与自动化产线结果；四块业务使用趋势、专题对比和状态图表呈现。
2. **人工质检**：总览、交付中心、验收中心、人力与分组。
3. **自动化质检**：总览先展示版本、质检项、准召率和专题配置；明细后续补齐。
4. **大模型质检**：总览先展示 POC Benchmark、能力成熟度和数据/评测资产；生产任务、版本配置作为工程页。
5. **专题数据质量**：总览展示 Golden 集和新增数据质量；新增数据明细承接近期交付视角。

## 必须可操作的用户路径

### 人工质检

- 从交付列表筛选"我的/临期/阻塞"任务，打开交付详情抽屉，查看阶段、行动项和风险。
- 进入验收中心，选择日期、scene、组与策略；执行 preview，修改参数使旧 preview 失效；确认后 execute；查看成功、跳过、失败与回查中状态。
- 查看通过/打回规则结论；PENDING 时禁止执行；EXECUTE 权限下才能确认通过或打回。
- 在人力页按项目、供应商、角色筛选，展示人员单项目归属与未进组预警。

### 大模型质检

- 在任务组概览下钻任务列表，使用前后游标模拟分页，打开任务详情抽屉查看六通道状态。
- 创建任务时切换"表单创建/文件上传"，验证必填项和提交状态。
- 批量 kill/clean，显示作用范围、危险等级与部分失败结果。
- 进入回收站执行恢复或永久删除。
- 在版本页按 model/video/prompt 筛选，创建或编辑 JSON 配置，并查看任务创建时版本冻结的说明。

## 共享组件与状态

| 共享组件 | 责任 |
|---|---|
| AppShell | 一级模块、二级导航、用户权限、全局环境与帮助 |
| FilterBar | 结构化筛选、保存视图、重置、数据新鲜度 |
| DataTable | 列配置、排序、行选择、空态、加载态、错误态 |
| DetailDrawer | 保持列表上下文的任务/交付详情下钻 |
| StatusBadge | 状态文字、图标和颜色三重编码 |
| AsyncActionPanel | preview、执行、部分成功、失败与回查 |
| ConfirmDialog | 危险操作范围、数量、影响和确认 |
| Toast/InlineAlert | 即时反馈与可恢复建议 |

## 视觉方向草案

拒绝纯"大屏炫技"，采用密集但克制的业务账本：全局使用浅色工作面、冷中性灰和单一钴蓝强调色，风险只使用红、黄、绿等语义色。信息主要通过分隔线、表格、紧凑面板和小圆角建立层级，不用深色侧栏或大量悬浮卡片制造"大屏感"。

签名元素是"质检脉络条"：页面顶部用一条紧凑的阶段/通道轨道展示当前对象从输入到最终状态的全过程；人工模块显示交付阶段，大模型模块显示六通道进度。它编码真实流程，不作装饰。

## 设计拨盘与实现约束

- `DESIGN_VARIANCE: 5`、`MOTION_INTENSITY: 2`、`VISUAL_DENSITY: 8`。
- 参考 Carbon 的企业工作台密度和状态表达，但单 HTML 原型不冒充官方 Carbon 实现。

- HTML + CSS + 原生 JavaScript，内置 Mock 数据，不依赖构建工具；ECharts 作为本地 vendor 资源。
- 使用 CSS 变量定义色彩、间距、圆角、阴影与状态；SVG 图标统一线宽，不使用 Emoji 作为结构图标。
- 键盘可达、焦点可见、普通文本对比度至少 4.5:1；支持 `prefers-reduced-motion`。
- 375 / 768 / 1024 / 1440 四档检查；桌面信息密度高，移动端改为抽屉导航与卡片化明细。
- 动效只用于抽屉、弹窗、Toast、按钮按压和状态迁移，控制在 100-260ms；高频筛选和键盘导航不动画。

## 分阶段交付

1. **P0 对齐**：确认 OneTrack 权限模式、首屏、真实通道名、Demo 深度和视觉方向。
2. **P1 骨架**：AppShell、平台总览、两模块路由与 Mock 数据层。
3. **P2 人工质检纵切**：交付详情 + 验收 preview/execute/refresh 完整交互。
4. **P3 大模型纵切**：任务列表/详情/六通道 + 版本配置 + 回收站。
5. **P4 体验收口**：响应式、键盘、错误/空态、动效与截图验收。

## 当前实现状态

- 已落盘：`demo/quality-check-platform.html`。
- 已覆盖：OneTrack 平台壳层、质检业务总览、人工质检总览、自动化质检总览、大模型质检总览和专题数据质量总览。
- 已实现交互：URL hash 深链、一级业务根导航、人工质检周/双周/月交付窗口、任务详情弹窗、移动端侧栏和 Toast 反馈。账号区只说明权限自动生效，不提供身份切换。
- 本轮升级目标：里程碑置顶；近期台账改为人工/抽检与自动化结果列并支持专题展开；四业务摘要提升为 ECharts 微型分析工作台；人工总览增加经营摘要、阶段轴、日期倒排轨道和全任务健康表。
- 本轮边界：交付中心、验收中心、人力与分组等下级明细页仅保留导航和占位，不得宣称已完成纵切交互。
- 已实现多维升级：里程碑置顶、近两周质量护航台账、专题展开、八张 ECharts、人工八阶段轴、共用日期尺倒排、接纳列配置保存、全量标注/验收队列。
- 人工总览细化：顶部两卡 1:1；通量图增加专题快捷聚焦、数量曲线和 Good 双轴曲线；质量/需求指标全部展开四专题；八阶段轴通过 URL 筛选深链下钻交付中心。
- 交付中心已提供需求对齐列视图和可编辑 Demo；验收中心、人力与分组仍为占位。
- 待后续真实接入：后端 API、SSO、真实权限、真实指标与业务文案审批。

## 已冻结的 Demo 假设

- 第一屏是团队视角的质检业务总览。
- 全员 OneTrack，不提供身份/视角切换；登录权限自动裁剪路由、区域、面板、按钮和数据范围。
- 六通道使用 `raw_img / label / pkl / video / prompt / inference` 及中文解释。
- Demo 不依赖 CDN；图表使用本地 `demo/vendor/echarts.min.js`，图标使用同文件 SVG symbol sprite。
- 做高保真可点击原型，并模拟数据变化、异步进度、部分失败和状态回查。

## 本轮重设计范围

- 从头设计左侧业务导航、顶部上下文、色彩、排版、面板和表格，不继承旧原型视觉。
- 优先完成质检业务总览和人工质检总览。
- 交付中心保留完整字段模型和下钻入口，本轮不在一页平铺全部字段。
- 其他模块总览先用业务真实结构表达，未知指标显式标注示例/待接真实口径。
