# 专家轨可观察记录

> 只记录输入、读取范围、工具、产物、用户决定和成本，不保存模型隐藏思维过程。

## 已发生

- 读取设计仓当前工作台、Blueprint、真实能力路线、M1 设计和私有 Eval Contract。
- 确认候选 Harness 基线为 `890e65f` 且工作区干净。
- 确认来源仓库基线为 `3cf393479d59dae57280df3c80a1ff213a936909`；工作区存在既有 `.gitignore` 修改及 `.chunks/`、`.claude/` 未跟踪项，专家轨保持只读。
- 读取两批 NotebookLM 导出清单，确认 23/24 字节数和 SHA-256 完全相同。
- 扫描冻结文档的标题结构，开始建立来源阅读计划。
- 全文读取历史快照 20、30—33 和人工语义来源 24。
- 读取目标设计 21、22 全文，以及 15、17—20、25 中与状态、采样、规则、Delta、快照和实现边界相关的段落。
- 全文读取当前 acceptance 路由、Ratio sampler、query/preview service、HTTP Schema、migration、聚焦测试，并补读 `AcceptanceRepository`。
- 使用 `python -B -m unittest tests.test_acceptance_vertical_slice -v` 运行 7 项测试，全部通过；来源仓测试前后 Git 状态一致。
- 生成第一版 24 个 Markdown 文件、15 个概念页的参考知识与两种产品视图；目标 Harness 的只读检查器返回 PASS。
- 用户第 1 轮指出新增 ChatGPT 质检项目来源、长文字缺少图表、入口层级和页面结构不直观、术语晦涩四类问题。
- 当前会话没有 ChatGPT Projects 私有内容读取工具，直接打开项目链接失败；已将其登记为认证阻塞来源，没有据此推断任何事实。
- 根入口、领域视图、流程、算法、结论与原型页增加 Mermaid 图和文字/表格说明，页面结构改为先给结论和全貌再下钻；用户页中的项目工作词已改成白话。
- 新增待接入来源记录后共 25 个 Markdown 文件、16 个概念页；目标 Harness 的只读检查器再次返回 PASS，且无警告。
- 用户将 25 份 ChatGPT 质检项目文件下载到 `knowledge/raw/quality_check/`，明确要求本轮和后续任务共同复用；已建立长期原始资料目录，不修改 `.txt` 原文件。
- 完成文件清单、格式、规模、资料集指纹和字节级重复检查；1 份 Delta 设计与旧 NotebookLM 原料完全重复，至少 11 个标题没有旧同名项。
- 定向读取验收相关材料后，补入过程监控、结果分析与返工跟踪主题，并暴露“通过数/已完成”与“通过数/已分配”的分母冲突，以及来源实施声明与固定代码提交不一致的问题。
- 第 3 轮参考成果共 26 个 Markdown 文件、17 个概念页；目标 Harness 只读检查再次通过，候选 Harness 仍停留在干净基线 `890e65f`。
- 用户批准“质检讲定位、人工质检讲全貌、验收纵向讲透”的学习矩阵，并进一步要求软件知识从业务动作映射到 Python 包、模块、类、函数和运行过程，按需使用 4+1 多视图，工程专题按需下钻。
- 第 4 轮新增人工质检生命周期、平台模块、核心对象，验收产品/数据/系统/Python/实现地图，以及数据库和 OBS 公共能力；旧监控与返工孤立页合并进产品工作台。
- 私有 Eval 与消费参考由 5 个问题升级为 12 个学习问题；参考成果共 35 个 Markdown 文件、26 个概念页，只读检查 PASS、0 error、0 warning。
- 重新运行固定来源的 7 项验收原型测试，全部通过；测试前后来源仓既有状态一致。候选 Harness 仍保持干净基线 `890e65f`，没有读取私有参考。

## 待记录

- 用户每轮审查决定和修正数量。
