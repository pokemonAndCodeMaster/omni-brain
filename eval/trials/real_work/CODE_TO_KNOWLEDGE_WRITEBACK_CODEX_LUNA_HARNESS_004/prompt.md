请在当前项目中完成一次真实的“代码变化 → 既有正式知识候选更新”。

现状：

- 当前仓库已经包含一套正式人工质检知识；它是父知识，不允许直接覆盖发布。
- 新的直接代码来源位于 `/home/yyh/project/qpl-gap-ref`，固定 commit 为 `35948aebf41b8772b77df18bd68ce9ff2d295344`。
- 这个来源是 `Quality Platform Lab` 本地实验系统，不是生产平台；它和知识库里已有的早期 Ratio 预览原型不是同一仓库或同一版本。
- 你可以使用当前项目的 AGENTS.md、Skills 和工具。不得读取 `/home/yyh/project/omni-brain/eval/`、任何 `eval/reference`、其他 Trial、`/home/yyh/project/omni-brain-knowledge-writeback-ref` 或其他模型候选。

用户希望最终获得：

1. 把当前代码中已经真实存在的 JSONB 快照、受控分析查询、任务→日期→组→标注员四级任务表、预期/实际验收分配、分配达成率和验收分配缺口，有机融入既有知识；
2. 学习者能分清业务规则、早期原型、当前本地实验、目标设计和生产未知；
3. 开发者能从产品入口理解系统结构、Python/SQL/Vue 责任、数据转换、关键公式和修改入口，而不是只得到源码链接或一两句摘要；
4. 根知识入口、领域视图、学习旅程、系统/来源导航和变更日志同步更新；
5. 原始代码和既有正式知识保持不变，候选可审查、可拒绝、可回滚；未经人工批准不要发布。

特别需要核清：

- Ratio 规划期的 Good/Bad 类别互补和执行后的分配缺口是什么关系；
- `expect_alloc`、`actual_alloc`、分配达成率和分配缺口分别回答什么问题；
- 分配缺口应在什么粒度计算，其他类别或位置的超额能否抵消；
- 任务表最大基础指标与五个固定问题选项怎样组合；
- Router、Schema、Service、Catalog、Repository、Vue feature 和 shared workbench 的实际职责边界；
- 当前 commit 没有包含哪些生产、外部执行和其他实验分支能力。

请自行按最低充分范围读取源码、文档和真实证据；不要为了覆盖率通读 node_modules、dist 或整个原料库。最终交给用户候选知识、产品视图和一份可读审查说明，并明确哪些事实已确认、哪些仍未知。你无需等待本轮用户逐步批准，但必须停在待审查状态。
