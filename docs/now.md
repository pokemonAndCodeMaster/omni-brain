# 当前实验工作台

> 最近更新：2026-07-26。

## 当前状态

知识摄入已能在发布前隔离候选并形成局部业务—代码纵切。V10 前向 Trial 已证明 `source-read` 可以完整展示关键长文并拒绝提前 `read_full`，但模型仍会漏掉自己已计划的上级来源，使知识从平台直接跳到用户焦点。V11 因此只增加来源选择与三层知识主线的自洽回环，当前状态是 `implemented_content_first_v11_awaiting_forward_test`；正式知识仍为空，也没有参考答案或业务评分点进入本项目。

## 当前可用

- OpenCode 原生根路由；
- `ingest-knowledge` 的盘点、候选编织、人工门禁、发布与立即消费流程；
- 面向弱模型的内容主线：先定义读者问题和“上级定位 → 主题全貌 → 用户焦点”，再匹配直接事实源、设计知识地图、从全貌下钻到代码/算法，最后从产品视图实际回答问题；
- `reader-answers.md` 逐题消费页：每个核心问题都必须给出可靠答案、部分答案、明确未知或确实不适用，并限制到产品视图和最多三篇规范页；
- `ingestion_workspace.py` 的机器来源清单、`source-read` 有界连续展示、`source-select` 关键来源绑定、Agent 覆盖声明、固定根入口、内容声明契约和结构检查；
- `completion.yaml` 的 `parent → subject → focus` 知识主线：每个适用层必须关联已读直接来源和独立正文主落点；
- 空 OKF Bundle、领域地图和两类产品视图入口；
- 标准 Markdown 相对链接约定与只读链接/入链检查；
- 已有任务案和本地 Git 来源范围能力。

## 当前未证明

- `source-select` 能否让弱模型真正处理自己认定的上级、主题和焦点来源，而不是随后批量筛掉；
- `knowledge_path` 能否阻止平台、主题全貌和用户焦点静默跳层或共用一篇薄焦点页；
- 关键来源完整阅读后，来源主题复核能否把已有事实稳定带入规范知识；
- 后置消费者能否按上级、主题全貌和用户焦点的原始问题范围判断完整性；
- Luna 能否为当前业务与软件结论选择匹配的直接事实源，并沿真实动作下钻 Python 实现；
- 真实材料能否形成高质量规范知识与产品视图；
- 算法细节、现实形态、冲突和未知能否保持准确；
- 用户审查负担是否可接受。

## 下一动作

冻结 V11 后，以同一 K0 Prompt、Batch 1、Luna medium 和人工审查停止点前向验证。重点观察模型是否选中并阅读三层直接来源、是否为人工质检全貌建立独立规范落点，以及内容能否超过 V10 的局部结果而不牺牲 Python 软件纵切和公共能力。
