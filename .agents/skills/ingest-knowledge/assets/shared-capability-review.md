# 公共能力抽取审查

公共能力候选的规范正文写入 `draft/knowledge/capabilities/<capability-id>.md`；不要为了容纳它在 `domains/` 新建 `shared` 或 `common` 根领域。领域页只保留本领域怎样消费该能力。

- **候选能力**：
- **当前领域页面**：
- **已知真实消费者**：
- **材料中是否有共享实现、公共 owner、接口或复用声明**：
- **候选维护责任人/来源**：

满足任一条件即可进入抽取审查，不要求先出现第二个外部领域：授权材料已有被当前主题消费的共享实现；两个真实模块/领域已经消费；材料给出明确公共 owner 或复用契约；用户明确说明它是团队公共能力。只因名字相似不能触发。

## 使用对比

| 维度 | 消费者 A | 消费者 B/公共契约 | 是否真正相同 |
|---|---|---|---|
| 输入 | | | |
| 输出 | | | |
| 不变量 | | | |
| 步骤/算法 | | | |
| 失败边界 | | | |
| 参数和语义 | | | |

## 拟抽取的公共核心

- 

## 各消费者必须保留的使用契约

- 消费者 A：
- 消费者 B（若有）：

已有共享实现时，owner 未知进入边界说明，不作为跳过公共候选页的理由。证据只够说明占位、声明或目标依赖时，也在 `capabilities/` 建立最小知识缺口页：写清预期用途、领域使用边界和未知，不得虚构当前实现。领域页只链接并保留本领域特有语义。

## 建议

- [ ] 不抽取：只有名称相似或证据不足
- [ ] 暂缓：尚无公共 owner、复用契约或两个真实消费者
- [ ] 抽取：公共核心独立成立，提交人工批准
