# 当前实验工作台

> 最近更新：2026-07-26。

## 当前状态

知识摄入已能在发布前隔离候选并形成局部业务—代码纵切。V10—V12 依次验证了长文有界输出、关键来源选择、三层知识落点和已读内容落页；同条件模型比较中 Kimi 与 GLM 5.1 均为 19/24，MiniMax M2.7 为 12/24，因此后续日常实验固定使用 GLM 5.1。V13 不再增加控制文件，而是压缩 `ingest-knowledge` 的运行上下文：模型只在进入相应阶段时读取所需模板，明确区分用户完整范围与材料中最强实现切片，并允许用 `partial` 诚实表达可用局部和剩余缺口。当前状态是 `implemented_content_first_v13_awaiting_forward_test`；正式知识仍为空。

## 当前可用

- OpenCode 原生根路由；
- `ingest-knowledge` 的盘点、候选编织、人工门禁、发布与立即消费流程；
- 面向弱模型的内容主线：先定义读者问题和“上级定位 → 主题全貌 → 用户焦点”，再匹配直接事实源、设计知识地图、从全貌下钻到代码/算法，最后从产品视图实际回答问题；
- `reader-answers.md` 逐题消费页：每个核心问题都必须给出可靠答案、部分答案、明确未知或确实不适用，并限制到产品视图和最多三篇规范页；
- `ingestion_workspace.py` 的机器来源清单、`source-read` 有界连续展示、`source-select` 关键来源绑定、Agent 覆盖声明、固定根入口、内容声明契约和结构检查；
- `completion.yaml` 的 `parent → subject → focus` 知识主线：每个适用层必须关联已读直接来源和独立正文主落点；
- `completion.yaml` 和内容消费均支持 `partial`：已有可用知识时保留证据页，同时明确已覆盖与仍缺范围，不再被迫在“完整”和“未知”之间二选一；
- Skill 模板按步骤渐进加载：内容计划、来源阅读、写作、产品视图、消费与审查不会在任务开始时一次性进入上下文；
- `inventory.md` 的已读内容落点：关键来源中的机制、条件、边界、冲突和未知必须在继续阅读前关联到具体页面/章节、开放问题或具体排除；
- 候选产品视图唯一位于 `draft/knowledge/views/`：具名视图带 frontmatter，`index.md` 只导航；检查器拒绝旧 `draft/views/` 和重复 frontmatter 键；
- 空 OKF Bundle、领域地图和两类产品视图入口；
- 标准 Markdown 相对链接约定与只读链接/入链检查；
- 已有任务案和本地 Git 来源范围能力。

## 当前未证明

- 渐进加载能否减少模板预读与无效上下文，并把注意力留给原始材料和知识正文；
- 显式完整范围与 `partial` 是否能阻止弱模型把最强代码纵切误写成整个业务范围已覆盖；
- 数据、软件结构和公共能力能否按独立变化形成稳定知识单元，而不在版本间回退；
- 后置消费者能否按上级、主题全貌和用户焦点的原始问题范围判断完整性；
- Luna 能否为当前业务与软件结论选择匹配的直接事实源，并沿真实动作下钻 Python 实现；
- 真实材料能否形成高质量规范知识与产品视图；
- 算法细节、现实形态、冲突和未知能否保持准确；
- 用户审查负担是否可接受。

## 下一动作

冻结 V13 后，以同一 K0 Prompt、Batch 1、GLM 5.1 和人工审查停止点前向验证。主要比较内容分数能否超过 V12 的 19/24、用户完整范围是否保留、OBS/数据库/Python 软件结构和开放问题是否进入知识、产品视图能否用最多三篇规范页回答真实问题；同时观察模板读取数量和命令成本，避免用更多流程换更薄正文。
